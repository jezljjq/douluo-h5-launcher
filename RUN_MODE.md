# 运行模式说明

**当前版本：前台串行模式**

---

## 1. 支持的运行模式

### 单账号运行

- 触发：GUI 下拉框选择具体账号 → 点击"单账号运行"
- 行为：运行选中账号的完整流程（OCR → 登录 → 校验）
- 适用：调试单个账号、验证流程正确性

### 当前层串行

- 触发：GUI 选择层级/分组 → 点击"当前层串行"
- 行为：逐个串行运行当前选中分组的账号
- 顺序：按收藏夹原始顺序
- 适用：批量登录同一层账号

### 全部串行

- 触发：点击"全部串行"
- 行为：逐个串行运行 `include_in_all=true` 的分组
- 顺序：按收藏夹解析出的分组顺序和分组内原始顺序
- 适用：全量批量登录

`层级=全部` 只影响账号列表显示，会显示全部已读取账号；它不代表“全部串行会跑全部分组”。全部串行运行前会在日志中输出本次执行分组和跳过分组。

---

## 2. 运行特性

| 特性 | 说明 |
|------|------|
| 运行模式 | 源码模式子进程隔离 / exe 模式同进程直接调用 |
| 实时状态 | STATUS/PASSPORT 实时推送 → GUI 表格 + 颜色标签 |
| 失败重试 | 任意步骤失败自动重试 1 次（始终走完整浏览器流程：关旧→开新→公告→按钮→输入→确认→校验） |
| 停止响应 | 200ms 内响应（所有等待可中断，不再等步骤完成） |
| 已登录跳过 | 500ms 二次确认，快速跳过已登录窗口 |
| 失败保留 | 失败截图保留到 `debug_ocr/_error/` + 浏览器窗口保留 |
| 文件日志 | `logs/run_时间.log` 完整日志 + GUI 简洁日志 |
| 残留清理 | 每个账号完成后 taskkill chromium 残留进程 |

---

## 3. 运行限制

| 限制 | 说明 |
|------|------|
| **前台模式** | 运行期间会短暂移动鼠标，请勿操作 |
| **严格串行** | 不支持并发，账号逐个执行 |
| **非后台** | 窗口必须在屏幕上可见 |
| **不可遮挡** | 浏览器窗口被遮挡时点击可能失败 |

---

## 4. 停止任务

- 点击"停止任务"按钮
- 当前正在执行的账号会尽可能完成当前步骤后停止
- 后续排队的账号不再执行

---

## 5. 窗口槽位运行状态

窗口管理区现在使用固定槽位概念：

- `slot_no` 是稳定窗口号，表示窗口位置、大小、标题和账号映射。
- `hwnd` 只是当前运行期间的临时句柄，窗口重开后会变化。
- `window_slots.json` 保存当前批次窗口槽位快照，用于单窗口补位和按槽位恢复布局；它不是永久配置。
- `window_manager_settings.json` 保存长期窗口管理参数，例如打开数量、排列方式、起点、窗口尺寸、间距和标题模板。
- 新版槽位快照会保存 `environment`、DPI、缩放比例、屏幕分辨率、`layout_params` 和 `slots`。
- “刷新槽位映射”只扫描当前带编号标题的游戏窗口并保存当前位置，不移动、不重命名、不启动窗口。
- 普通“排列窗口”在存在有效 `window_slots.json` 时，默认按已保存槽位恢复布局，不按 `hwnd` 或枚举顺序重新排序。
- “重新生成槽位”是高风险操作，必须二次确认后才会全局排列、全局编号并覆盖槽位文件。

修复窗口时如果目标 slot 不在 `window_slots.json` 中，会先尝试从当前窗口标题编号补齐；如果目标窗口已经关闭，再按固定排列参数推导位置。只有历史位置、当前窗口标题和固定参数都不可用时，才提示需要刷新槽位或手动处理。

`window_slots.json` 是本地运行状态文件，不提交 Git，不进入发布包；换机器、换布局、4K/100% 与 2K/150% 等 DPI/缩放环境变化、或删除该文件后，需要刷新槽位映射或重新生成槽位。

如果全部 H5 窗口已经关闭，再执行“批量启动窗口”，这是新窗口会话：普通旧 `hwnd` 快照不再作为恢复依据，程序会按当前 `window_manager_settings.json` 重新排列、重命名并生成新的 `window_slots.json`。

---

## 6. 收藏夹路径运行规则

- `automation_settings.json` 中的 `bookmark_file` 是用户上次保存的收藏夹路径，启动时优先使用。
- 当前默认浏览器为 Edge；没有保存路径时，程序默认使用 `Edge Default` 的 Bookmarks。
- Chrome / Edge 自动探测只用于候选提示，不允许 Chrome 因为文件存在就静默覆盖 Edge 路径。
- 保存配置时会记录 `bookmark_file`、`bookmark_browser`、`bookmark_profile`，用于区分 Edge / Chrome / profile。
- 读取收藏夹失败时，会在日志中输出当前读取的 Bookmarks 路径和检测到的一级目录。
- exe 模式读取 `dist\Launcher\automation_settings.json`，同样遵守以上规则，不从源码配置或默认 Chrome 路径静默覆盖用户配置。

---

## 7. 收藏夹动态分组与全部串行分组设置

- 收藏夹根目录为 `账号`。
- 层级下拉动态读取 `账号` 目录下的真实子目录，不再写死第一层到第四层。
- 根目录下数字收藏项作为 `单层账号`。
- 根目录下非数字收藏项，例如 `JS`，会跳过并记录日志。
- 自定义分组，例如 `存钻`、`备用`、`小号`，只要内部有有效游戏链接就会显示。
- 自定义分组内账号标题不要求纯数字，按收藏夹原始顺序映射窗口。
- 分组设置窗口用于配置哪些分组参与 `全部串行`，配置保存到 `automation_settings.json` 的 `account_group_settings`。
- 新发现分组默认 `include_in_all=false`，不会自动加入全部串行。
- 未勾选分组仍可通过 `当前层串行` 或 `单账号运行` 执行。

方式一账号表列顺序为：

```text
层级 / 收藏编号 / 窗口号 / 参与全部串行 / 本次通行证 / 链接 / 状态 / 耗时
```

`参与全部串行` 列只显示 `是` / `否`，通行证、状态和耗时分别写入各自列。

---

## 8. 运行方式

### 从源码运行

```powershell
cd <项目路径>
python main.py
```

### 从打包 exe 运行

```powershell
.\scripts\build_exe.bat          # 先打包
cd dist\斗罗大陆H5上号器
.\斗罗大陆H5上号器.exe            # 运行
```

**注意**：exe 模式下单账号和批量均走同进程直接调用（`sys.frozen` 检测）。Dm 点击仍需 `py -3.14-32`。详见 [BUILD.md](BUILD.md)。
