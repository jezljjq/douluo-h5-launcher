# 下一步计划

**日期：2026-05-17**

---

## 1. 当前阶段完成情况

| 模块 | 状态 |
|------|------|
| 通行证提取 | 复制优先，OCR 兜底 |
| OCR 兜底规则 | 已加强（低置信度和 c/e 混淆必须失败） |
| 登录程序窗口状态判断 | 稳定（图像特征 ROI 三态分类） |
| 后台截图 | 稳定 |
| 通行证按钮模板匹配 | 稳定（坐标缓存加速） |
| Dm 前台点击 | 稳定 |
| 剪贴板粘贴输入 | 稳定 |
| 公告关闭 | 稳定 |
| 确认按钮视觉定位 | 稳定 |
| 单账号完整流程 | ~7.5s 稳定 |
| 前台串行批量 | 待验证 |
| 失败自动重试 | 稳定（超时→重试整流程） |
| 校验轮询 | 稳定（快速状态检测 + 10s 预算） |
| 方式二账号密码登录 | ✅ 已开发 |
| CSV 导入 | ✅ 稳定（encoding 自动检测） |
| 方式二单账号流程 | ✅ 稳定（OCR→浏览器登录→公告→Dm输入→校验） |
| 方式二串行批量 | ✅ 已对齐方式一：快速提交 + 统一校验 + 失败重登 |
| CSV 路径记忆 | ✅ 启动自动加载 |
| 截图清理 | ✅ _error/ 保留最新10张 |
| 日志清理 | ✅ logs/ 保留最新2份 |
| 方式一+方式二耗时统计 | ✅ 日志+表格双显示 |
| 窗口管理区 | ✅ 已接入 |
| 批量启动/识别/排列/关闭 | ✅ 已实现，31 窗口全量重启/排列/重命名已现场验证 |
| 标题模板/重命名/排列后自动编号 | ✅ 已实现 |
| 窗口管理参数记忆 | ✅ 已现场验证，全部窗口关闭后按当前 `window_manager_settings.json` 重新生成槽位 |
| 窗口槽位映射 `window_slots.json` | ✅ 已实现，slot 11/15 历史验证通过；2026-06-07 31 窗口重启和 slot 31 补位验证通过 |
| 刷新槽位映射 | ✅ 已实现，只扫描保存，不移动、不重命名、不启动窗口 |
| 普通排列按槽位恢复 | ✅ 已实现，避免按 hwnd / 枚举顺序重排 |
| 重新生成槽位 | ✅ 源码修复，高风险操作需二次确认且窗口数必须等于目标数 |
| 槽位完整性保护 | ✅ 源码修复，禁止 30 窗口覆盖 31 profile；写入前备份 + tmp + replace |
| 修复窗口缺失槽位兜底 | ✅ 源码修复，按当前 profile、备份、legacy、当前窗口、固定参数推导顺序解析 |
| 槽位环境和 layout 校验 | ✅ 已实现，保存 DPI/缩放/屏幕分辨率/layout_params，不一致时提示 |
| 收藏夹动态分组 | ✅ 已实现，动态读取 `账号` 目录真实子目录，支持 `存钻` 等自定义分组 |
| 全部串行分组设置 | ✅ 已实现，`include_in_all=true` 分组才参与全部串行，新分组默认不参与 |
| 串行按钮范围隔离 | ✅ 源码验证，单账号/当前层/全部串行范围固定；全部串行层级非“全部”时先阻止提示 |
| 全部串行 run_plan 预检 | ✅ 源码验证，启动前一次性检查需要窗口、当前窗口、缺少窗口 |
| 账号列表列顺序 | ✅ 已修复，参与全部串行/通行证/状态/耗时列不再错位 |
| 游戏图标拖拽配置 | ✅ POC 已验证，主程序接入 `tkinterdnd2`；GUI 默认普通权限启动以避免 Explorer 拖拽被拦截 |
| 辅助软件窗口误识别过滤 | ✅ 已修复，31 个真实游戏窗口 + 1 个辅助软件窗口时只识别 31 个游戏窗口 |
| 停止任务/关闭清理 | ✅ 已验证通过 |
| 通行证弹窗坐标文件缓存 | ✅ 已验证，跨账号/子进程复用 |
| 合并 Dm chain | ✅ 已验证，命中缓存后约 2.4s |
| 批量快速登录 + 统一校验 | ✅ 已验证，9 个单层账号最终 9/9 成功 |
| 批量快速登录结果分类 | ✅ 已修复并检核通过：已登录跳过直接成功，不进待复核 |

**当前版本：前台串行阶段性完成版。方式一、方式二、窗口管理区、源码模式、exe 模式均已完成当前阶段验证。方式一与方式二批量流程均已改为快速提交 + 统一校验 + 失败重登；单账号运行仍保留完整校验。快速提交阶段已区分 `already_logged_in` / `submitted` / `failed`，已登录账号直接成功，不参与统一校验，`submitted` 不直接算最终成功。**

---

## 2. 2026-05-13/14 已完成的优化

- canvas 轮询替代 2s 固定等待（打开页面 2.9→0.9s）
- 视觉优先弹窗检测（免 OCR）
- 校验快速状态轮询（前 1s 不跑 OCR，之后每 3s 一次）
- 校验 10s 时间预算 + 超时走整流程重试
- OCR 预处理优化（票数阈值安全性修复）
- 关闭公告/按钮/输入 等待参数压缩
- 候选替换改为超时重试（不用错误候选干扰）

---

## 3. 后续优先级

1. 保持当前前台串行稳定版，不再在收尾阶段继续加功能。
2. 后续先确认 31 个窗口批量上号长期稳定性。
3. 基于 `window_slots.json` 继续观察 31 窗口槽位映射长期稳定性；注意它是当前批次快照，长期排列参数以 `window_manager_settings.json` 为准。
4. 最后再做批量启动 → 自动排列 → 自动上号联动。
5. UI 美化后置，不要优先于核心流程稳定性。

### 已收口的高优先级问题

- 停止任务机制已修复并验证通过：点击“停止任务”会终止当前账号子进程、清理 `dm_click_helper.py`、清理本次 Playwright/Chromium 相关进程，并停止后续账号。
- 关闭程序机制已修复并验证通过：窗口关闭前会执行停止和子进程清理，GUI 关闭后不允许残留子进程继续移动鼠标。
- 批量快速登录 + 统一校验已验证通过：失败账号只重登失败账号，9 个单层账号最终全部成功。
- 批量状态分类 bug 已修复并检核通过：日志中出现“检测到已登录界面，跳过”的账号，不再显示“已输入确认，标记待复核”，而是直接计入成功。
- 方式二批量校验策略已对齐方式一并通过源码模式验证：CSV 批量采用“快速提交 → 统一校验 → 失败重登”，`already_logged_in` 不参与统一校验，`submitted` 不直接算最终成功，失败账号只重登失败项。
- 本阶段方式二批量策略调整已提交 Git。
- 任意窗口槽位修复已现场验证通过：slot 11 补位只启动 1 个新窗口，未重新排列、未重新编号、未移动其它窗口。
- 缺失槽位兜底已现场验证通过：slot 15 历史位置缺失时，可先刷新槽位映射；目标窗口已关闭时可按固定排列参数推导目标位置，不再直接失败。
- 普通“排列窗口”已改为优先按 `window_slots.json` 恢复槽位；“重新生成槽位”单独作为高风险入口，需要二次确认才覆盖槽位文件。
- 2026-06-07 现场验证：先通过窗口管理层关闭当前 31 个 H5 窗口并确认窗口数降为 0，再批量启动 31 个窗口；新会话按当前固定参数排列/重命名并重新生成槽位；普通按槽位恢复 31/31 成功且不覆盖槽位文件；slot 31 单窗口补位只更新目标 hwnd，其它 30 个槽位不变。
- 2026-06-10 源码修复：目标 31 但当前 30 个窗口时，刷新槽位映射和重新生成槽位均会阻止；修复 slot 29 可从当前 profile、备份、legacy 或固定参数推导坐标；槽位覆盖写入会备份并原子替换；高风险窗口操作改为 worker 线程执行并禁用相关按钮。
- 收藏夹动态分组已实现：层级下拉不再写死，`存钻` 等自定义分组可读取；根目录数字项作为 `单层账号`，根目录非数字项跳过并记录日志。
- 全部串行分组设置已实现：`层级=全部` 只显示全部账号，`全部串行` 只运行 `include_in_all=true` 的分组；未勾选分组仍可当前层串行或单账号运行。
- 串行按钮范围已源码修复：`当前层串行` 只运行当前层级；`全部串行` 只运行已启用分组，且当前层级不是 `全部` 时先阻止并提示语义差异。
- 全部串行 run_plan 预检已源码修复：启动前输出执行分组、账号数、需要窗口、最大窗口号、当前桌面窗口、缺少窗口；窗口不足立即阻止，不再跑到中途才发现窗口缺失。
- 账号列表列错位已修复：`参与全部串行` 显示 `是/否`，通行证、状态、耗时分别写入对应列。
- 源码模式和 exe 模式资源路径一致性已修复并验证：exe 不再查找 `_internal\playwright\driver\package\.local-browsers`，统一使用 `%LOCALAPPDATA%\ms-playwright`。
- 已新增 `launcher-regression-guard` 防回归技能；后续修改上号器前必须检查已登录/OCR/qr_page/unknown/停止清理/exe 路径等硬规则。
- 辅助软件窗口误识别已修复：窗口识别统一使用 `is_game_window()` / `list_game_windows()`，优先按配置的 `X5Game.exe` 进程路径过滤，编号窗口严格匹配 `斗罗大陆H5-数字号`；辅助软件、工具窗口和上号器自身不能参与识别、排列、槽位刷新、槽位重建、修复窗口、批量启动前检测或运行前预检。

## 4. 代码层面已实现但未充分现场验证

- 2026-06-10 窗口槽位修复需现场验证：31 缺 1 修复 slot 29、30 窗口禁止重新生成、30 窗口禁止刷新、0 窗口新会话批量启动 31 自动排列保存、完整 31 窗口重新生成槽位不卡 UI。
- 2026-06-15 窗口识别过滤需在真实桌面继续观察：31 个真实游戏窗口 + 1 个标题以 `斗罗大陆H5` 开头的辅助软件时，识别窗口、排列窗口、刷新槽位映射、重新生成槽位均不得计入或操作辅助软件。
- 2026-06-09 串行按钮范围与 run_plan 预检需做真实 GUI 场景验证：`存钻` 当前层串行只要求窗口 1-9；`存钻` 下点击全部串行先提示；全部串行窗口不足立即列出缺口；只勾选 `存钻` 后全部串行不找窗口 10。
- 复制通行证在大批量窗口下的稳定性。
- OCR 新兜底规则在大量失败样本下的表现。

---

## 5. 中期方向（真后台模式）

需解决以下技术问题后启动：

| 问题 | 可能方向 |
|------|----------|
| Dm Win11 兼容 | 大漠新版本 / 64 位注册 |
| 真后台点击 | DD 虚拟驱动 / 其他内核方案 |
| 真并发 | 需先解决后台点击，再引入多线程 |

---

## 6. 禁止

- 不要回退到旧的点击/OCR 方案
- 不要恢复“低票数接受最佳候选”的 OCR 逻辑
- 不要在 `c/e` 混淆时自动猜通行证
- 前台串行稳定版不要引入真并发
- 不要在未解决后台点击前开启批量并发
- 不要修改 OCR 主逻辑、状态判断、已登录跳过
- 文档任务只允许改文档，不允许顺手改代码
- 打包必须单独确认，不得在普通开发任务中自动打包
- 禁止只做协作式 `stop_event`，不清理实际子进程
- 禁止误杀所有 `python.exe`，只清理本项目相关子进程
