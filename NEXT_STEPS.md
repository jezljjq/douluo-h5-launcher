# 下一步计划

**日期：2026-05-15**

---

## 1. 当前阶段完成情况

| 模块 | 状态 |
|------|------|
| 通行证提取 | 复制优先，OCR 兜底 |
| OCR 兜底规则 | 已加强（低置信度和 c/e 混淆必须失败） |
| 登录程序窗口状态判断 | 稳定（图像特征 ROI 三态分类） |
| 后台截图 | 稳定 |
| 通行证按钮模板匹配 | 稳定（坐标缓存加速） |
| Dm 前台点击 | 稳定 |
| 剪贴板粘贴输入 | 稳定 |
| 公告关闭 | 稳定 |
| 确认按钮视觉定位 | 稳定 |
| 单账号完整流程 | ~7.5s 稳定 |
| 前台串行批量 | 待验证 |
| 失败自动重试 | 稳定（超时→重试整流程） |
| 校验轮询 | 稳定（快速状态检测 + 10s 预算） |
| 方式二账号密码登录 | ✅ 已开发 |
| CSV 导入 | ✅ 稳定（encoding 自动检测） |
| 方式二单账号流程 | ✅ 稳定（OCR→浏览器登录→公告→Dm输入→校验） |
| 方式二串行批量 | ✅ 已实现 |
| CSV 路径记忆 | ✅ 启动自动加载 |
| 截图清理 | ✅ _error/ 保留最新10张 |
| 日志清理 | ✅ logs/ 保留最新2份 |
| 方式一+方式二耗时统计 | ✅ 日志+表格双显示 |
| 窗口管理区 | ✅ 已接入 |
| 批量启动/识别/排列/关闭 | ✅ 已实现，部分已验证 |
| 标题模板/重命名/排列后自动编号 | ✅ 已实现 |
| 窗口管理参数记忆 | 代码层面已实现，未现场验证 |
| 停止任务/关闭清理 | ✅ 已验证通过 |

**当前版本：方式一+方式二双模式 + 窗口管理区。主流程仍为前台串行模式。新增窗口管理参数记忆、批量启动后自动排列再重命名等能力已实现，但部分尚未现场实机验证。**

---

## 2. 2026-05-13/14 已完成的优化

- canvas 轮询替代 2s 固定等待（打开页面 2.9→0.9s）
- 视觉优先弹窗检测（免 OCR）
- 校验快速状态轮询（前 1s 不跑 OCR，之后每 3s 一次）
- 校验 10s 时间预算 + 超时走整流程重试
- OCR 预处理优化（票数阈值安全性修复）
- 关闭公告/按钮/输入 等待参数压缩
- 候选替换改为超时重试（不用错误候选干扰）

---

## 3. 后续优先级

1. 现场验证复制通行证是否稳定。
2. 现场验证窗口管理参数记忆。
3. 现场验证批量启动、自动排列、自动编号标题。
4. 确认 8 个窗口批量上号稳定。
5. 再确认 31 个窗口批量上号稳定。
6. 后续再做窗口管理排序结果与上号器窗口定位融合。
7. 最后再做批量启动 → 自动排列 → 自动上号联动。
8. UI 美化后置，不要优先于核心流程稳定性。

### 已收口的高优先级问题

- 停止任务机制已修复并验证通过：点击“停止任务”会终止当前账号子进程、清理 `dm_click_helper.py`、清理本次 Playwright/Chromium 相关进程，并停止后续账号。
- 关闭程序机制已修复并验证通过：窗口关闭前会执行停止和子进程清理，GUI 关闭后不允许残留子进程继续移动鼠标。

## 4. 代码层面已实现但未充分现场验证

- 窗口管理参数记忆。
- 批量启动后自动排列再重命名。
- 复制通行证在大批量窗口下的稳定性。
- OCR 新兜底规则在大量失败样本下的表现。

---

## 5. 中期方向（真后台模式）

需解决以下技术问题后启动：

| 问题 | 可能方向 |
|------|----------|
| Dm Win11 兼容 | 大漠新版本 / 64 位注册 |
| 真后台点击 | DD 虚拟驱动 / 其他内核方案 |
| 真并发 | 需先解决后台点击，再引入多线程 |

---

## 6. 禁止

- 不要回退到旧的点击/OCR 方案
- 不要恢复“低票数接受最佳候选”的 OCR 逻辑
- 不要在 `c/e` 混淆时自动猜通行证
- 前台串行稳定版不要引入真并发
- 不要在未解决后台点击前开启批量并发
- 不要修改 OCR 主逻辑、状态判断、已登录跳过
- 文档任务只允许改文档，不允许顺手改代码
- 打包必须单独确认，不得在普通开发任务中自动打包
- 禁止只做协作式 `stop_event`，不清理实际子进程
- 禁止误杀所有 `python.exe`，只清理本项目相关子进程
