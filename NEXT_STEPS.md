# 下一步计划

**日期：2026-05-13**

---

## 1. 当前阶段完成情况

| 模块 | 状态 |
|------|------|
| OCR 通行证提取 | 稳定（>50% 票数阈值，低票数走手动输入） |
| 登录程序窗口状态判断 | 稳定（图像特征 ROI 三态分类） |
| 后台截图 | 稳定 |
| 通行证按钮模板匹配 | 稳定（坐标缓存加速） |
| Dm 前台点击 | 稳定 |
| 剪贴板粘贴输入 | 稳定 |
| 公告关闭 | 稳定 |
| 确认按钮视觉定位 | 稳定 |
| 单账号完整流程 | ~7.5s 稳定 |
| 前台串行批量 | 待验证 |
| 失败自动重试 | 稳定（超时→重试整流程） |
| 校验轮询 | 稳定（快速状态检测 + 10s 预算） |
| 方式二账号密码登录 | 暂未开发 |

**当前版本：前台串行速度优化版。单账号 ~7.5s（从 17.8s 优化 58%）。**

---

## 2. 2026-05-13 已完成的优化

- canvas 轮询替代 2s 固定等待（打开页面 2.9→0.9s）
- 视觉优先弹窗检测（免 OCR）
- 校验快速状态轮询（前 1s 不跑 OCR，之后每 3s 一次）
- 校验 10s 时间预算 + 超时走整流程重试
- OCR 预处理优化（票数阈值安全性修复）
- 关闭公告/按钮/输入 等待参数压缩
- 候选替换改为超时重试（不用错误候选干扰）

---

## 3. 短期可做

- 验证前台串行批量（当前层/全部层）的实际表现
- 增加批量运行结束后的汇总统计
- 输入阶段 Dm 子进程改同进程调用（省 ~1.5s）
- 增加 OCR 失败时的手动输入回退（已支持，需确认体验）

---

## 4. 中期方向（真后台模式）

需解决以下技术问题后启动：

| 问题 | 可能方向 |
|------|----------|
| Dm Win11 兼容 | 大漠新版本 / 64 位注册 |
| 真后台点击 | DD 虚拟驱动 / 其他内核方案 |
| 真并发 | 需先解决后台点击，再引入多线程 |

---

## 5. 禁止

- 不要回退到旧的点击/OCR 方案
- 前台串行稳定版不要引入真并发
- 不要在未解决后台点击前开启批量并发
- 不要修改 OCR 主逻辑、状态判断、已登录跳过
