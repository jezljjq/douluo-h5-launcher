# 当前问题和限制

**日期：2026-05-17**

---

## 0. 当前真实状态（2026-05-17 阶段性完成版）

### 已验证过的能力

- 方式一：通行证上号流程曾通过验证。
- 方式二：账号密码 + 通行证上号流程曾通过验证。
- OCR、公告关闭、Dm 输入、Playwright 流程曾通过回归验证。
- 窗口识别、排列、关闭曾通过验证。
- 窗口排列/关闭权限不足问题已定位为管理员权限问题，并已处理默认管理员启动相关逻辑。
- 停止任务和关闭程序清理子进程问题已验证通过。
- 文件级通行证弹窗坐标缓存已验证生效，可跨账号、跨源码模式子进程复用。
- 合并 Dm chain 已验证生效，命中缓存后链路为 `click 通行证按钮 → wait → click 输入框 → type 通行证 → click 确认`。
- 当前层串行 / 全部串行的“批量快速登录 + 统一校验 + 失败重登”已验证。
- 方式二批量校验策略已对齐方式一：CSV 批量也采用“快速提交 → 统一校验 → 失败重登”。
- 9 个单层账号最终全部成功：总 9，成功 9，失败 0。
- 方式一通行证上号已验证通过。
- 方式二账号密码 + 通行证上号已验证通过。
- 源码模式和 exe 模式均已测试通过。
- 固定参数排列和行数列数排列均已支持。
- 单层账号和四层账号已隔离。
- 已新增 `D:\Ai\skills\launcher-regression-guard\SKILL.md` 防回归技能。

### 后续仍需扩大样本验证

- 31 个窗口批量上号长期稳定性。
- 更多窗口尺寸和不同虚拟桌面下的排列/登录稳定性。
- OCR 新兜底规则在大量失败样本下的表现。

### 当前仍未完成或后置

- 窗口管理排序结果与上号器窗口定位真正融合。
- 运行前自动排列窗口。
- 批量启动窗口后自动进入上号流程。
- UI 美化。

---

## 0.1 通行证获取与 OCR 当前规则

当前通行证获取优先级：

1. 双击选中登录程序窗口中的 8 位通行证，`Ctrl+C` 复制，读取剪贴板。
2. 复制失败后才进入 OCR 兜底。

OCR 不再作为第一优先级。原因是 OCR 存在单字符误识别风险，例如真实值 `4425cbaa` 可能被识别为 `4425ebaa`，错误位置为 `c` 被识别为 `e`。

OCR 兜底规则：

- 禁止“低票数接受最佳候选”。
- 新增失败类型：`OCR_LOW_CONFIDENCE`、`OCR_AMBIGUOUS_CHAR`。
- 必须输出候选结果、候选票数、字符级投票、存疑位置、是否接受、失败类型。
- OCR 低置信度或存在 `c/e` 混淆时必须失败或进入人工处理，不能假成功。

原则：宁可真实失败，也不要错误输入通行证。

---

## 0.2 窗口管理当前状态

窗口管理功能已经从独立工具迁入上号器。上号器项目内部已有独立窗口管理模块，不应依赖独立窗口管理器项目路径。独立项目 `DLH5WindowManager` 被移动或删除，理论上不应影响上号器内部窗口管理功能。

当前窗口管理功能包括：

- 批量启动
- 识别
- 按已保存槽位恢复排列
- 关闭
- 重命名
- 自动编号标题
- 参数记忆
- 任意窗口槽位修复 / 单窗口补位
- 刷新槽位映射
- 重新生成槽位（高风险，需二次确认）

### 窗口槽位机制（2026-06-10 更新）

窗口号现在应按固定槽位 `slot` 理解。`slot_no` 保存窗口位置、大小、标题、账号映射和当前状态；`hwnd` 只是当前运行期间的临时句柄，窗口重开后会变化。

本地状态文件 `window_slots.json` 保存“当前批次窗口槽位快照”，字段包括：

- `slot_no`
- `hwnd`
- `title`
- `x` / `y`
- `width` / `height`
- `account_layer`
- `account_index`
- `account_name`
- `status`
- `updated_at`

新版 `window_slots.json` 还包含 `environment`、DPI、缩放比例、屏幕分辨率和 `layout_params`。它不是永久配置；长期排列参数保存在 `window_manager_settings.json`。

`window_slots.json` 不提交 Git，不进入发布包。换机器、换显示器布局、4K/100% 与 2K/150% 等缩放环境变化、删除槽位文件或槽位文件不完整后，需要刷新槽位映射或重新生成槽位。

当前已验证：

- `repair_window_slot(slot_no)` 语义已支持任意槽位，不写死 11 或 13。
- slot 11 已现场补位成功。
- slot 15 在历史位置缺失时已现场验证：可根据刷新槽位映射或固定排列参数兜底，不再直接失败。
- “刷新槽位映射”只保存当前带编号窗口位置，不移动、不重命名、不启动窗口。
- 单窗口补位只启动 1 个新窗口，不移动、不关闭、不重命名其它窗口。
- 普通“排列窗口”在存在有效 `window_slots.json` 时默认按槽位恢复布局，不按 `hwnd` 或枚举顺序重新排序。
- 普通“重命名”在存在槽位文件时只按槽位标题修正，不全局重新编号。
- 只有点击“重新生成槽位”并确认后，才允许全局排序、全局排列、全局重命名并覆盖 `window_slots.json`。
- 全部窗口关闭后重新批量启动时，按当前 `window_manager_settings.json` 重新排列、重命名并生成新的 `window_slots.json`，不沿用旧 `hwnd` 快照。
- 普通按槽位恢复会校验当前环境和 `layout_params`，环境/DPI/缩放或排列参数不一致时输出提醒，不静默覆盖槽位快照。
- 已新增完整性硬保护：当前 profile 目标窗口数为 31 时，如果当前只识别到 30 个窗口，“重新生成槽位”和“刷新槽位映射”都会直接阻止，不能用 30 个窗口覆盖 31 profile。
- 底层保存函数也会校验 expected_count，GUI 之外调用也不能写入不完整槽位。
- 每次覆盖当前 profile 槽位文件前会先备份到 `slots/backups/`，再写 `.tmp`，最后 replace；写入失败不破坏旧文件。
- “修复窗口”不再要求当前 profile 槽位文件完整。slot 解析顺序为：当前 profile 文件、最近备份、legacy `window_slots.json`、当前桌面同编号窗口、固定参数推导。
- 固定参数推导公式：`row=(slot_no-1)//per_row`，`col=(slot_no-1)%per_row`，`x=start_x+col*offset_x`，`y=start_y+row*offset_y`，宽高使用当前固定参数。
- “重新生成槽位”“刷新槽位映射”“排列窗口”“修复窗口”“批量启动窗口”会禁用相关按钮；长耗时排列、重命名、扫描、保存放到 worker 线程，避免 UI 未响应。

剩余风险：

- 当前 profile 文件和备份都丢失时，固定参数模式仍可推导缺失 slot；按行数模式无法推导固定坐标，需关闭全部窗口后按当前参数重新批量启动生成完整槽位。
- 如果当前虚拟桌面不可见部分窗口，槽位恢复只能处理当前可枚举窗口；不要跨虚拟桌面强行恢复。
- 不要把普通“排列窗口”和“重新生成槽位”混用。
- 禁止写死窗口坐标、编号、分辨率、缩放比例、路径或槽位数量。

### 收藏夹路径规则（2026-06-07 更新）

当前已修复收藏夹路径被 Chrome 候选静默覆盖的问题。

- 程序启动时优先使用 `automation_settings.json` 中保存的 `bookmark_file`。
- 当前默认浏览器为 Edge；没有保存路径时，默认使用 `Edge Default` 的 Bookmarks。
- Chrome / Edge 自动探测只作为候选日志，不允许 Chrome 静默覆盖 Edge。
- 保存配置时会记录 `bookmark_file`、`bookmark_browser`、`bookmark_profile`。
- 读取收藏夹失败时会输出当前读取路径和检测到的一级目录，便于判断是否选错浏览器或 profile。
- exe 模式同样读取 `dist\Launcher\automation_settings.json`，不能回退覆盖为源码配置或默认 Chrome 路径。

### 收藏夹动态分组与全部串行分组设置（2026-06-09 更新）

当前已完成动态分组读取和全部串行分组自选。

- 收藏夹根目录为 `账号`。
- 层级下拉会动态读取 `账号` 目录下真实存在的子目录，不再写死第一层到第四层。
- `存钻`、`备用`、`小号` 等自定义分组只要包含有效游戏链接就会显示。
- 自定义分组内账号标题不要求纯数字，`z1` / `z2` 等会按收藏夹原始顺序读取和映射窗口。
- 根目录下数字收藏项继续作为 `单层账号`；根目录下非数字收藏项，例如 `JS`，会跳过并记录日志。
- `层级=全部` 只表示账号列表显示全部账号。
- `全部串行` 只执行 `include_in_all=true` 的分组。
- 分组设置窗口可勾选哪些分组参与全部串行，新发现分组默认不参与全部串行。
- 分组配置保存到 `automation_settings.json` 的 `account_group_settings` 字段。
- 未勾选分组仍可通过 `当前层串行` 和 `单账号运行` 执行。

账号列表表格列错位已修复。方式一账号表列顺序固定为：

```text
层级 / 收藏编号 / 窗口号 / 参与全部串行 / 本次通行证 / 链接 / 状态 / 耗时
```

`参与全部串行` 只显示 `是/否`，`本次通行证` 只显示通行证，`状态` 只显示运行状态，`耗时` 只显示耗时。UI 表格不作为业务核心数据源，避免新增列导致上号数据错位。

### 串行按钮语义与运行前预检（2026-06-09 更新）

当前已修复“全部串行”和“当前层串行”的范围歧义。

- `单账号运行`：只运行当前账号下拉框或表格选中的账号。
- `当前层串行`：只运行当前层级下拉框选中的分组，不读取 `include_in_all`，不受其它分组勾选状态影响。
- `全部串行`：只运行“全部串行分组设置”中 `include_in_all=true` 的分组，不等于当前层级，也不等于当前表格显示内容。
- 当前层级不是 `全部` 时点击 `全部串行`，会先阻止并提示：如需运行当前层请点击 `当前层串行`；如需运行全部启用分组请先切换层级为 `全部`。
- `全部串行` 启动前会生成 run_plan，日志输出本次执行分组、账号数、需要窗口、最大窗口号、当前桌面 H5 窗口和缺少窗口。
- 窗口不足会在启动前一次性阻止，不再运行到窗口 10 时才报错。
- `存钻` 当前层串行保持自定义分组映射：`z1→窗口1`，`z2→窗口2`，直到 `z9→窗口9`；不会因为其它分组存在而映射到窗口 40-49。

源码验证：

- `python -m unittest discover -s tests -v`：44 tests OK。
- `python -m compileall .\douluo_launcher .\tests .\main.py`：通过。

仍未现场验证：

- 需要在真实桌面 9 个窗口场景下手动点 GUI 验证四个场景：`存钻` 当前层串行、`存钻` 下点击全部串行阻止提示、全部串行窗口不足预检失败、只勾选 `存钻` 后全部串行只要求窗口 1-9。

---

## 0.3 停止任务与关闭程序安全机制

停止任务机制已修复：

- 点击“停止任务”后，不再只是设置 `stop_event`。
- 会强制终止当前账号运行子进程。
- 会清理 `dm_click_helper.py` 子进程。
- 会清理本次 Playwright/Chromium 相关进程。
- 不再继续后续账号。

关闭程序机制已修复：

- 点击窗口右上角关闭时，会先执行停止任务和子进程清理。
- GUI 关闭后，不允许残留子进程继续移动鼠标。
- 避免 `_drain_ui_queue` 在窗口关闭后继续 `after` 回调。

当前安全规则：

- 运行中点击停止，必须立即停止。
- 关闭程序前必须清理子进程。
- 禁止只做协作式 `stop_event`，不清理实际子进程。
- 禁止误杀所有 `python.exe`，只清理本项目相关子进程。

---

## 0.4 快速登录缓存与统一校验当前状态

当前新增能力：

- 通行证弹窗坐标缓存文件：`debug_ocr/passport_dialog_pos_cache.json`。
- 缓存 key 使用 viewport，例如 `960x720`。
- 缓存内容包括 `button`、`input`、`confirm`、`updated_at`。
- 后续账号命中缓存后使用合并 Dm chain，减少 32 位 helper 子进程调用次数。
- 合并 Dm chain 实测耗时约 `2.4s`。

当前批量流程：

- 单账号运行：仍执行完整校验，成功才显示成功。
- 方式一当前层串行 / 全部串行：快速提交所有账号后统一校验。
- 方式二 CSV 批量串行：已对齐方式一，同样快速提交所有账号后统一校验。
- 统一校验失败账号才进入下一轮重登，且只重登失败项。
- `重新次数` 限制统一校验和失败重登轮数；如果提前全部成功，不继续执行剩余校验轮次。
- `already_logged_in` 直接计入成功，不参与统一校验。
- `submitted` 只表示已完成提交并等待复核，不能直接算最终成功。

当前安全规则：

- `qr_page` 不能判成功。
- `unknown` 不能判成功。
- 截图失败不能判成功。
- 只有明确 `logged_in` 才成功。
- 失败账号只重登失败账号，不全量重跑。

---

## 1. 2026-05-13 速度优化记录

### 优化成果

单账号 QR 登录耗时从 ~17.8s → 10.5s → **7.5s**（-58%，warm start）。冷启动 ~9.5s。

| 步骤 | 优化前 | 优化后 | 手段 |
|------|--------|--------|------|
| 打开页面 | 2.9s | 0.9s | canvas 轮询替代固定 2s 等待 |
| 关闭公告 | 1.3s | 0.6s | 减少固定 sleep |
| 点击+输入 | 2.5+2.4=4.9s | 4.1s | Dm 合并链式调用（按钮+输入一次子进程） |
| 校验 | 0.2s | 0.2-0.4s | 快速状态检测 + 10s 时间预算 |

### 关键改动
- `dm_click_helper.py` 链式调用支持 `wait` 步骤
- 按钮点击+输入合并为一次 `_dm_chain` 调用（省一次 32位Python 子进程启动）
- `detect_login_page_state`：else 分支改为 `logged_in`（非二维码即已登录）
- `_quick_login_state()`：仅截图+图像特征，不做 OCR
- `_is_passport_dialog_visible_by_ocr`：视觉检测在前 + gray_ratio<0.10 快速排除
- `passport_btn_viewport` 回退坐标修正为 `(683,234)`
- `passport_input_ratio` / `confirm_button_ratio` 根据实际位置校准
- OCR 失败二次确认后弹窗手动输入（首次失败也会触发）

---

## 2. 2026-05-12 修复记录

### 已修复：`subprocess.Popen` monkey-patch 导致 Playwright/asyncio 崩溃

**症状**：点"全部串行"后报 `TypeError: function() argument 'code' must be code, not str`

**根因**：`automation.py` 模块级 `subprocess.Popen = _no_console_popen` 用 function 替换了 class，导致 asyncio 的 `windows_utils.Popen(subprocess.Popen)` 继承失败。

**修复**：
1. 用 class 继承替代 function：`class _NoConsolePopen(_original_popen)` 保持 Popen 是类
2. Playwright 导入前临时恢复原始 Popen，导入后恢复补丁（`run()` 和 `run_game_flow()` 两处）

### 已修复：重试时相同通行证跳过完整浏览器流程

**症状**：第1次登录失败 → 重试时 OCR 读到相同通行证 → 直接抛异常跳过，不重新打开浏览器

**修复**：删除"相同通行证→跳过"的检查，重试始终走完整浏览器流程（关旧浏览器→开新→公告→按钮→输入→确认→校验）

### 运行效果

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 成功率 (31账号) | 24/31 (77%) | 29/31 (94%) |
| 耗时 | 843秒 | 366秒 |

---

## 3. 当前已知限制

### 前台模式限制

| 限制 | 影响 | 说明 |
|------|------|------|
| 非后台运行 | 运行时不能操作电脑 | Dm 前台点击必须独占鼠标 |
| 非真并发 | 32 个账号需严格串行完成 | 前台点击不能并发 |
| 鼠标会物理移动 | 干扰用户正常工作 | 临时方案，后续需升级为真后台 |
| 窗口必须可见 | 不能最小化或遮挡 | Dm MoveTo 需要可见屏幕坐标 |

### 当前风险

| 风险 | 影响 | 说明 |
|------|------|------|
| 登录程序窗口状态判断 | 二维码页可能误判为已登录 | `detect_login_page_state` 依赖阈值调优 |
| OCR 通行证识别 | 部分截图 OCR 质量不稳定 | 复制优先；复制失败后 OCR 兜底，低置信度必须失败 |
| 窗口标题变化 | `H5-{n}-` 格式可能改变 | 后续需支持坐标排序映射 |

### 已修复：批量快速登录状态分类

- 现象：登录前已经判断为 `logged_in` 并输出“检测到已登录界面，跳过”的账号，GUI 曾继续显示“已输入确认，标记待复核”。
- 原因：快速提交阶段原先只返回布尔值，GUI 无法区分“已登录跳过”和“已提交待复核”。
- 当前规则：
  - `already_logged_in`：直接标记“已登录/成功”，不进入统一校验和重登列表。
  - `submitted`：已输入通行证并点击确认，才进入“待复核”。
  - `failed`：提交前失败，进入失败/重登列表。
- 安全要求：不要把 `already_logged_in` 再拉回通行证复制、OCR 或重登流程；统一校验只处理 `submitted` 账号。

### 方式二已开发

- 方式二（账号密码 + 通行证上号）已完成，与方式一共用同一 GUI
- 支持 CSV 导入（encoding 自动检测 utf-8-sig/gbk/gb2312/utf-8）
- 支持 Playwright DOM 账号密码登录 + Dm 通行证输入
- 支持自动重试（2次）和串行批量
- CSV 路径记忆：启动时自动加载上次导入的 CSV
- 密码安全：仅内存使用，不写入日志/文件/子进程
- 2026-05-30：方式二批量校验策略已对齐方式一，改为“快速提交 → 统一校验 → 失败重登”。
- `already_logged_in` 不参与统一校验，`submitted` 不直接算最终成功，失败账号只重登失败项。
- 本阶段已完成源码模式验证并提交 Git。

### OCR 字符混淆

- Tesseract 对 hex 字符识别存在混淆：c↔0、c↔e、ff↔ee
- 复制优先已经降低 OCR 风险
- OCR 兜底已增加字符级投票和 `c/e` 强拦截
- 新兜底规则仍需更多失败样本验证

### Chrome Canvas 限制

| 限制 | 影响 | 说明 |
|------|------|------|
| isTrusted 检测 | Playwright/CDP 合成事件全部被拦截 | Chrome 安全策略，无法绕过 |
| GPU 渲染管线 | SendMessage/PostMessage 不转发到游戏 | Chrome 绕过 Windows 消息机制 |
| Dm BindWindow 崩溃 | 7.2607 + Win11 = SIGSEGV | 36/36 全模式崩溃，无法后台绑定 |

### 其他限制

| 限制 | 影响 |
|------|------|
| 需 32 位 Python 子进程 | 环境依赖复杂，需同时安装 64/32 两个 Python |
| 分辨率适配 | 当前仅验证 960×720 viewport |
| OCR 偶发失败 | ~5% 概率需重试，投票+候选互换已覆盖大部分场景（已知 f↔7 混淆） |

---

## 4. 已验证不可用的方案

| 方案 | 测试结果 | 不可用原因 |
|------|----------|------------|
| Dm 7.2607 BindWindow | 36/36 崩溃 | Win11 不兼容 |
| Playwright canvas click | 全部被拦截 | isTrusted 检测 |
| CDP Input.dispatchMouseEvent | 全部被拦截 | isTrusted 检测 |
| PostMessage WM_LBUTTONDOWN | 无效果 | Chrome 不转发 |
| SendMessage 到 Chrome_RenderWidgetHostHWND | 无效果 | GPU 渲染绕过 |
| JS 注入覆盖 isTrusted | 无效 | 注入时机太晚/额外检测 |
| 二维码定位 + 裁剪 OCR | 不稳定 | 位置不固定 |
| Dm OpenClipboard API | 句柄无效 | 多进程冲突 |
| Dm KeyPressChar 逐字输入 | 焦点不确定 | 窗口可能不在前台 |

---

## 5. 当前可用但有限制的方案

| 方案 | 限制 |
|------|------|
| Dm 无绑定前台点击 | 会移动鼠标，需独占 |
| clip.exe + Dm Ctrl+V 粘贴 | 需浏览器在前台 |
| BitBlt 后台截图 | 部分窗口可能黑屏，需 ImageGrab 回退 |
| Tesseract 全图 OCR | ~10% 失败率，自动重试覆盖 |
| Popen 子进程隔离（仅源码模式） | 每个账号需启动新 Python 进程（~2s 开销）；exe 模式同进程调用无需此开销 |

---

## 6. 当前必须遵守

- 不要回退二维码定位/裁剪 OCR
- 不要恢复“低票数接受最佳候选”的 OCR 逻辑
- 不要在 `c/e` 混淆时自动猜测通行证
- 不要对浏览器页面 OCR 通行证
- 不要继续尝试 Dm BindWindow（当前版本不兼容 Win11）
- 不要使用 Playwright/CDP/PostMessage/SendMessage 点击游戏 canvas
- 不要开启真并发（前台模式不支持）
- 不要把停止任务退回成只设置 `stop_event`
- 不要用“杀全部 python.exe”的方式清理子进程

---

## 7. 分辨率与窗口尺寸依赖

### 当前版本定位

**当前窗口尺寸稳定版，不是完全自适应分辨率版本。**

### 浏览器 viewport

- 固定为 **960×720**，通过 `automation_settings.json` 的 `window_width` / `window_height` 配置
- Playwright 启动参数 `--window-size` 和 `viewport` 均使用此配置
- 模板匹配、视觉定位的 ROI 区域基于 viewport 比例计算（支持调整配置值，但仅在 960×720 下验证过）

### 登录程序窗口

- WindowsForms 窗口固定 **320×540**，由登录程序自身决定
- OCR 截图按窗口实际尺寸（`capture_window_background` 自适应）
- 标题匹配规则：`斗罗大陆H5-{游戏窗口号}-`（如 `斗罗大陆H5-9-伊导科技`）

### 固定坐标/区域

| 项目 | 类型 | 说明 |
|------|------|------|
| 模板匹配 | 固定模板 27×36 | `template_passport_btn.png`，仅在 960×720 下提取 |
| 按钮回退坐标 | 固定 viewport 坐标 | `passport_btn_viewport: (683, 290)`，模板匹配失败时使用 |
| 公告关闭 | 固定 canvas 坐标 | `page.mouse.click(740, 680)`，非比例计算 |
| 输入框/确认按钮 | **比例计算** | `window_width * ratio`，配置可调但仅验证 960×720 |
| 视觉定位 ROI | **比例计算** | `int(width * 0.25)` 等，配置无关但未在其他分辨率测试 |
| 窗口排列 | 固定间距 | `gap_x=20, gap_y=40`，仅适用于 960×720 |

### 后续如需支持不同分辨率

需单独处理：
- 模板按比例缩放匹配（`cv2.matchTemplate` 多尺度）
- 公告关闭改为比例坐标或视觉定位
- 按钮回退坐标按 viewport 比例重新计算
- 在不同分辨率下重新验证视觉定位 ROI

---

## 8. 窗口定位方式

### 自动枚举 + 标题匹配（非写死 hwnd）

1. 根据层级和收藏编号计算游戏窗口号（如 第二层 收藏1 → 窗口9）
2. `select_login_window_by_game_no(game_window_no)` 枚举当前系统所有可见窗口
3. 按标题匹配规则 `H5-{number}-` 自动选中对应登录程序窗口
4. 浏览器窗口同样自动枚举：`_find_game_browser_window()` 按标题关键词匹配

窗口句柄不在代码中写死，运行时自动获取。但如果窗口标题格式变化（如不再包含 `H5-{number}-`），需调整 `window_title_matches_game_no` 中的匹配规则。
