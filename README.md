# 斗罗大陆H5上号器（前台串行稳定版）

**当前阶段：前台串行阶段性完成版。方式一、方式二、窗口管理区、批量快速登录、统一校验、停止清理、源码模式和 exe 模式均已完成当前阶段验证。**

> 项目级开发规则见 [CLAUDE.md](CLAUDE.md)。

---

## 1. 项目定位

自动登录"斗罗大陆H5"游戏，支持最多 32 个账号（4 层 × 8 账号），前台串行逐个完成登录。

### 核心流程

```
前台辅助模式：登录程序窗口复制优先获取通行证（OCR 兜底）
→ 浏览器打开游戏页
→ 关闭公告
→ 模板匹配定位通行证按钮
→ Dm 前台点击打开弹窗
→ 剪贴板粘贴输入通行证
→ 视觉定位确认按钮并点击
→ 回到登录程序窗口校验 QR 码是否消失
→ 成功 / 失败重试
```

---

## 2. 当前已完成

| 模块 | 状态 | 方案 | 文档 |
|------|------|------|------|
| 前台通行证获取 | ✅ 已调整 | 复制优先，OCR 兜底；OCR 不再低置信度放行 | [OCR_SUCCESS.md](OCR_SUCCESS.md) |
| 后台通行证获取 | ✅ 已调整 | 禁用复制/读取链路，直接使用 red_bar_box 局部 OCR 多证据 | [BACKGROUND_CAPABILITY.md](BACKGROUND_CAPABILITY.md) |
| 后台截图 | ✅ 稳定 | BitBlt + ImageGrab 回退 | [OCR_SUCCESS.md](OCR_SUCCESS.md) |
| 通行证按钮定位 | ✅ 稳定 | OpenCV 模板匹配 | [CLICK_SOLUTION.md](CLICK_SOLUTION.md) |
| Dm 前台点击 | ✅ 稳定 | Dm 无绑定 MoveTo + LeftDown/LeftUp | [CLICK_SOLUTION.md](CLICK_SOLUTION.md) |
| 剪贴板粘贴输入 | ✅ 稳定 | clip.exe + Dm Ctrl+V | [CLICK_SOLUTION.md](CLICK_SOLUTION.md) |
| 公告关闭 | ✅ 稳定 | Playwright canvas 右下角点击 | — |
| 确认按钮定位 | ✅ 稳定 | 视觉黄色按钮检测（y最小+x最大） | [CLICK_SOLUTION.md](CLICK_SOLUTION.md) |
| 单账号完整流程 | ✅ 稳定 | 7 步流程 + 失败自动重试 | — |
| 前台串行批量 | ✅ 稳定 | Popen 子进程隔离 + 实时状态同步 | [RUN_MODE.md](RUN_MODE.md) |
| GUI 状态同步 | ✅ 稳定 | Treeview + 颜色标签 + 实时刷新 | [GUI_STATUS_FLOW.md](GUI_STATUS_FLOW.md) |
| 调试截图管理 | ✅ 已整理 | _tmp / history / latest_* 三级管理 | [DEBUG_IMAGE_POLICY.md](DEBUG_IMAGE_POLICY.md) |
| 失败重试 | ✅ 已实现 | 任意失败自动重试 1 次 | — |
| 收藏夹映射 | ✅ 稳定 | 4层×8编号→游戏窗口号1-32 | — |
| 方式二（账号密码+通行证） | ✅ 稳定 | CSV导入+Playwright DOM登录+Dm输入 | [DESIGN_METHOD2.md](DESIGN_METHOD2.md) |
| CSV 导入 | ✅ 稳定 | encoding 自动检测 + 路径记忆 | — |
| 耗时统计 | ✅ 已实现 | 分阶段计时 + 日志+表格双显示 | — |
| 截图/日志清理 | ✅ 已实现 | _error/ 保留10张，logs/ 保留2份 | — |
| 窗口管理区 | ✅ 已接入 | 批量启动、识别、按槽位恢复排列、关闭、重命名、单窗口补位、槽位完整性保护、参数记忆 | [docs/WINDOW_MANAGER_AND_PASSPORT_MILESTONE.md](docs/WINDOW_MANAGER_AND_PASSPORT_MILESTONE.md) |
| 停止任务/关闭清理 | ✅ 已验证 | 停止时终止账号子进程、清理 dm_click_helper.py 和 Chromium | [CLICK_SOLUTION.md](CLICK_SOLUTION.md) |
| 通行证弹窗坐标缓存 | ✅ 已验证 | `debug_ocr/passport_dialog_pos_cache.json` 按 viewport 缓存 button/input/confirm；按钮点击后必须先校验弹窗出现，缓存失效会清空并重匹配 | [CLICK_SOLUTION.md](CLICK_SOLUTION.md) |
| 批量快速登录 + 统一校验 | ✅ 已验证 | 当前层/全部串行先快速提交，统一校验后只重登失败账号；9 个单层账号最终 9/9 成功 | [docs/LAUNCHER_FINAL_MILESTONE.md](docs/LAUNCHER_FINAL_MILESTONE.md) |
| 串行按钮范围与预检 | ✅ 源码验证 | 单账号/当前层/全部串行范围隔离；全部串行先生成 run_plan 并一次性校验窗口缺口 | [CURRENT_ISSUES.md](CURRENT_ISSUES.md) |
| GUI 日志区 | ✅ 已修复 | 默认窗口 1160x820，最小窗口 1080x760；日志区固定最小高度并显示 8 行，缩小时不再被表格压缩 | [REGRESSION_TESTS.md](REGRESSION_TESTS.md) |
| 配置区易用性 | ✅ 已实现 | 客户模式只暴露选择游戏图标/程序、自动查收藏夹、账号目录下拉；技术路径放入高级配置 | [REGRESSION_TESTS.md](REGRESSION_TESTS.md) |
| 游戏窗口识别过滤 | ✅ 已修复 | 统一使用辅助关键字排除 + 编号标题后缀识别 + 进程路径强确认，辅助软件不会计入游戏窗口 | [ERROR_HISTORY.md](ERROR_HISTORY.md), [REGRESSION_TESTS.md](REGRESSION_TESTS.md) |
| 源码 / exe 一致性 | ✅ 已验证 | exe 使用 `dist/Launcher/上号器.exe`，发布包内置 `ms-playwright` Chromium，不依赖目标电脑用户缓存 | [BUILD.md](BUILD.md) |
| 防回归体系 | ✅ 已加固 | 历史事故台账 + 自动化测试映射 + 打包前硬检查；没有回归测试的修复不算完成 | [ERROR_HISTORY.md](ERROR_HISTORY.md), [REGRESSION_TESTS.md](REGRESSION_TESTS.md) |

### 2.0.0 游戏路径选择规则

游戏路径不能写死，也不要求客户知道真实 `X5Game.exe` 在哪里。

- 主界面显示为“游戏程序”，支持把桌面游戏图标、`.lnk` 快捷方式、`X5Game.exe` 或游戏安装目录拖入游戏程序区域。
- 拖拽采用公开库 `tkinterdnd2`；已废弃的原生 `WM_DROPFILES` Tk 子类化方案会导致闪退，禁止回退。
- GUI 默认不再启动时自动管理员提权，因为 Windows 会阻止普通资源管理器向管理员窗口拖入文件；如用户主动以管理员运行，拖拽可能被系统权限隔离拦截。
- 同时保留“选择游戏图标/程序”按钮作为兜底。
- 可以直接选择 `.exe`。
- 可以拖入或选择桌面/开始菜单 `.lnk` 快捷方式；程序会解析快捷方式真实目标，只保存真实 exe 路径，不保存 `.lnk`。
- 可以在取消文件选择后选择游戏安装目录；程序会优先查找目录下的 `X5Game.exe`。
- 识别成功后主界面只显示“已识别游戏程序：<真实 exe 路径>”。
- 识别成功后输入框和“已识别游戏程序”状态必须同步显示同一个真实 exe 路径。
- 无效文件、空快捷方式、目标不存在、目标不是 exe 时会弹窗提示，不静默失败。

### 2.0.1 收藏夹路径规则

收藏夹路径是用户配置，不是浏览器自动探测结果。

- 主界面采用客户模式：只显示“自动查找收藏夹”、“收藏夹候选”、“账号目录”和“读取账号”。
- `Bookmarks` 原始路径、`bookmark_root_path`、兼容目录名和自动化设置路径只在“高级配置”折叠区显示，默认不暴露给普通客户。
- 程序启动时优先读取 `automation_settings.json` 中上次保存的 `bookmark_file`。
- 如果已保存路径存在且可读，必须保持原路径，不允许因为 Chrome Bookmarks 存在而静默切换。
- 点击“自动查找收藏夹”会扫描 Edge / Chrome 的 `Default` 和 `Profile *` 目录，并在下拉框显示 `Edge - Default - 发现 2 个账号目录`、`Chrome - Profile 1 - 发现 1 个账号目录` 等客户可读候选。
- 收藏夹候选下拉不得显示 `C:\Users\...\Bookmarks` 这类原始路径。
- 自动扫描只生成候选，不静默覆盖用户保存路径；只有没有保存路径且仅有一个候选时才自动选择，并写日志。
- 成功选择或读取收藏夹后，会保存 `bookmark_file`、`bookmark_browser`、`bookmark_profile`、`bookmark_root_path` 和 `bookmark_root_display_name`。
- 切换收藏夹候选时，会清空旧账号目录候选和旧账号列表，避免 `Chrome` 候选继续使用 `Edge` 扫描出的目录。
- 读取账号前会校验账号目录候选所属 Bookmarks 文件与当前收藏候选一致；不一致时必须重新选择账号目录。
- 读取收藏夹失败时，日志会输出当前读取的 Bookmarks 路径和检测到的目录，便于判断是否选错浏览器或 profile。

### 2.0.2 收藏夹动态分组与全部串行过滤

收藏夹账号目录不再强制叫 `账号`。程序会扫描 Bookmarks 内所有包含有效游戏链接的目录，并让用户在“账号目录”下拉框选择。

- 账号目录候选显示为 `收藏栏 / 斗罗大陆 - 31个账号，包含4个分组`、`收藏栏 / 斗罗大陆 / 第一层 - 8个账号`、`收藏栏 / 存钻 - 9个账号`、`其它收藏夹 / 游戏 - 8个账号`。
- 游戏链接直接放在收藏栏或其它收藏夹时，也会生成 `收藏栏（直接链接）- 5个账号`、`其它收藏夹（直接链接）- 3个账号` 候选。
- 选择父目录时，其下直接子目录作为分组读取。
- 选择子目录时，只读取该子目录账号；例如选择 `收藏栏 / 斗罗大陆 / 存钻` 时，只读取 `存钻`。
- 老配置只保存 `bookmark_root_name="账号"` 时仍兼容；如果扫描到唯一匹配目录，会迁移为新的 `bookmark_root_path` / `bookmark_root_display_name`。保存了 `bookmark_root_path` 时优先按路径恢复。
- 保存的账号目录路径不存在时，不会静默回退到 `账号`，会重新扫描并提示选择。
- 程序读取账号时使用内部结构化 `root_path` 定位目录，允许两个不同路径下存在同名 `存钻`，不会只按名字误读另一个目录。
- 读取失败时会清空或标记旧账号列表，避免下方表格继续显示上一次成功加载的 `第一层`。
- 运行区层级来自当前已加载账号；未读取时显示 `未读取`，只加载 `存钻` 时默认层级切到 `存钻`。
- 任意子目录都可作为分组，例如 `第一层`、`第二层`、`第三层`、`第四层`、`存钻`、`备用`、`小号`。
- 自定义分组内账号标题不要求纯数字，`z1`、`z2`、`z3` 等只要是有效游戏链接都会读取。
- 自定义分组内部按收藏夹原始顺序显示和映射窗口，不强行按数字排序。

`层级=全部` 显示的是“全部串行范围”，只包含 `include_in_all=true` 的分组账号；`include_in_all=false` 的分组不会出现在全部列表里。点击 `全部串行` 时使用同一份过滤后的账号列表，避免 UI 显示一套、实际运行另一套。分组设置按钮可选择哪些分组参与全部串行，新发现分组默认不参与全部串行。该设置保存到 `automation_settings.json` 的 `account_group_settings` 字段。

未勾选分组不会进入 `层级=全部` 的列表，也不会进入 `全部串行`，但仍可切换到具体分组后通过 `当前层串行` 单独运行，也可通过 `单账号运行` 运行其中某个账号。

2026-06-09 语义修正：

- `单账号运行` 只运行账号下拉框或表格选中的一个账号。
- `当前层串行` 在当前层级为具体分组时只运行该分组，不读取 `include_in_all` 勾选状态。例如层级为 `存钻` 时，只运行 `存钻 z1-z9`，只要求窗口 `1-9`。
- `当前层串行` 在当前层级为 `全部` 时运行当前列表中显示的账号，也就是 `include_in_all=true` 的账号。
- `全部串行` 只运行“全部串行分组设置”中 `include_in_all=true` 的所有分组；数据来源与 `层级=全部` 的账号列表一致。
- 当前层级不是 `全部` 时点击 `全部串行` 会先阻止并提示：如需运行当前层请点 `当前层串行`；如需运行全部启用分组请先切换层级为 `全部`。
- `全部串行` 真正启动前会生成 run_plan，日志输出将执行分组、账号数、需要窗口、最大窗口号、当前桌面可见 H5 窗口和缺少窗口；窗口不足会立即阻止，不再跑到中途才发现窗口 10 不存在。

### 2.0.3 后台能力检测报告

程序启动时会输出当前后台能力结论，并提供 [BACKGROUND_CAPABILITY.md](BACKGROUND_CAPABILITY.md) 作为详细报告。当前默认流程明确标记为 `前台辅助模式`：前台仍保持复制优先、OCR 兜底；后台模式禁止 WM_GETTEXT、UIA、后台 Ctrl+C 和剪贴板读写，通行证提取直接使用 red_bar_box 局部 OCR 多证据。黑屏保护暂不实现。

### 2.0.4 后台登录模式实验框架

运行区新增 `前台辅助模式` / `后台登录模式（实验）` 选择。默认仍是 `前台辅助模式`。后台实验模式当前使用 `BackgroundOperator` 的 hwnd 截图 / PostMessage 点击 / WM_CHAR 输入能力，方式一单账号 live 已验证成功；后台当前层串行已完成 `存钻` 前 2 个账号小范围 live 验证并成功；后台全部串行已接入但仍等待更大范围 live 验证。后台串行只做串行，逐个复用 `BackgroundSingleAccountRunner`，并发固定为 1。

后台模式通行证获取已废弃 WM_GETTEXT、UIA、后台复制、剪贴板 marker、剪贴板读取和剪贴板恢复链路；真实验证结论为这些复制/读取路径没有实际价值。后台模式现在直接后台截图并使用 red_bar_box 局部 OCR 多证据提取本次通行证，失败时保存现场并报错。前台辅助模式不变，仍是复制优先、OCR 兜底。

后台串行中，单个账号失败只记录失败并继续下一个；只有用户停止任务、依赖预检失败、读取设置失败或运行前预检失败才会中断整轮。后台成功窗口默认保留，方便人工确认；后台流程不调用 `SetForegroundWindow`，不使用全局鼠标/键盘。

方式一账号表当前列顺序固定为：

```text
层级 / 收藏编号 / 窗口号 / 参与全部串行 / 本次通行证 / 链接 / 状态 / 耗时
```

- `参与全部串行` 只显示 `是` / `否`。
- `本次通行证` 只显示本轮提取到的通行证。
- `状态` 只显示 `未开始`、`运行中`、`成功`、`失败` 等状态。
- `耗时` 只显示耗时。
- UI 表格只用于展示，不作为上号核心数据源，避免以后新增列导致业务数据错位。

### 2.1 当前窗口管理区能力

窗口管理功能已经从独立工具迁入上号器内部模块。上号器不应依赖独立项目 `DLH5WindowManager` 的本地路径；即使独立项目移动或删除，上号器内部窗口管理功能理论上也不应受影响。

当前窗口管理区支持：

- 游戏路径
- 打开数量
- 启动间隔(ms)
- 启动后自动排列
- 排列后自动编号标题
- 标题模板
- 重命名
- 窗口宽度
- 窗口高度
- 每行数量
- 起点 X
- 起点 Y
- 横向偏移
- 纵向偏移
- 批量启动窗口
- 识别窗口
- 排列窗口
- 刷新槽位映射
- 重新生成槽位
- 任意窗口槽位修复 / 单窗口补位
- 关闭窗口

窗口管理参数记忆保存到上号器项目内部独立配置文件 `window_manager_settings.json`。当前已通过源码模式现场验证：关闭全部 H5 窗口后重新批量启动 31 个窗口，会按当前 `window_manager_settings.json` 的固定排列参数重新排列、重命名并生成新的槽位快照。

### 2.1.0 游戏窗口识别规则

所有窗口管理入口必须统一使用 `douluo_launcher.window_manager.is_game_window()` / `list_game_windows()` 过滤窗口，禁止各处自行用标题包含判断。

- 标题包含 `辅助`、`全自动辅助`、`任务开关`、`公共设置`、`日常设置`、`代理设置`、`上号器`、`工具` 时必须直接排除。
- 配置了游戏程序路径时，优先读取 hwnd 对应进程 exe 路径，并与配置中的真实 `X5Game.exe` 路径比对；进程路径匹配是强确认条件，但进程路径不匹配不能一票否决。
- 已编号游戏窗口标题必须从当前 UI 的“标题模板”动态生成识别正则；例如模板 `斗罗大陆H5-{index}号` 识别 `斗罗大陆H5-1号`、`斗罗大陆H5-1号-扫码登录`，模板 `DLH5-{index}` 识别 `DLH5-1`、`DLH5-1-扫码登录`。
- 进程路径不匹配但标题符合编号规则、窗口尺寸接近当前窗口管理配置时，仍可作为真实游戏窗口候选。
- 未编号窗口只允许在明确允许未编号场景下识别由当前标题模板推导出的精确未编号标题。
- 禁止回退到 `title.startswith("斗罗大陆")`、`"斗罗大陆H5" in title` 或从标题任意位置提取数字。
- 每次窗口识别会把候选窗口诊断写入 `logs/window_detection_detail.log`，包含 hwnd、title、class_name、pid、process_path、rect、标题命中、辅助关键字、进程路径匹配、accept/reject 和原因。
- `SetWindowPos` 失败必须记录 hwnd/title/pid/process_path/管理员状态/当前 rect/目标位置/错误码文字；错误码 5 必须提示 Windows 拒绝访问和权限不一致原因。排列或重新生成槽位时只要任意移动失败，禁止写入槽位文件。
- 修改窗口识别逻辑后必须运行 `tests/test_window_manager.py` 中的辅助软件误识别防回归测试，以及完整 `python -m unittest discover -s tests -v`。

历史事故：桌面有 31 个真实游戏窗口和 1 个标题以 `斗罗大陆H5` 开头的辅助软件时，旧逻辑把辅助软件计入第 32 个窗口，导致排列窗口提示 `目标 31，当前 32`。当前防回归要求：同场景识别结果必须是 31，辅助软件不能被移动、重命名或写入槽位文件。
二次回归：真实窗口标题格式必须跟随 UI 标题模板，不能在代码里写死 `斗罗大陆H5`；由辅助软件启动导致 hwnd 进程路径不等于配置的 `X5Game.exe` 时，也不能被过滤成 0。

### 2.1.1 窗口槽位机制

窗口号现在应理解为固定槽位 `slot`，而不是“当前枚举窗口列表里的第几个”。例如 `slot 11` 表示 11 号窗口的位置、大小、标题、账号映射和当前运行状态。

槽位稳定字段包括：

- `slot_no`
- `title`
- `x` / `y`
- `width` / `height`
- `account_layer`
- `account_index`
- `account_name`
- `status`

`hwnd` 只是当前运行期间的临时窗口句柄。窗口关闭、卡死后重新打开，`hwnd` 会变化；稳定依据是 `slot_no`、位置、尺寸、账号映射和标题模板。

槽位映射保存到本地运行状态文件 `window_slots.json`。该文件是“当前批次窗口槽位快照”，用于单窗口补位和按槽位恢复布局；它不是永久配置，不提交 Git，也不应进入发布包。长期排列参数以 `window_manager_settings.json` 为准。

新版 `window_slots.json` 会保存 `environment`、DPI、缩放比例、屏幕分辨率、`layout_params` 和 `slots`。4K/100%、2K/150% 等不同显示环境不能混用错误槽位；环境或布局参数不一致时，普通槽位恢复会输出提醒，不允许静默覆盖槽位快照。

`window_slots.json` 是本地运行状态文件，不提交 Git，不进入发布包；换机器、换显示器布局、关闭全部窗口后开启新批次、或文件丢失后，需要通过“刷新槽位映射”或“重新生成槽位”重新建立映射。

刷新槽位映射：

- 只枚举当前桌面可见、通过统一游戏窗口过滤函数确认的严格编号斗罗大陆H5窗口。
- 读取当前 `hwnd`、标题、坐标和尺寸，并写入 `window_slots.json`。
- 不移动窗口。
- 不重命名窗口。
- 不启动新窗口。
- 不覆盖没有重新扫描到的其它历史槽位。

单窗口补位流程：

1. 读取 `window_slots.json`。
2. 找到目标 `slot_no`。
3. 如果 slot 存在，直接使用历史位置。
4. 如果 slot 不存在，尝试从当前窗口标题编号补齐槽位。
5. 如果目标窗口已关闭，尝试根据固定排列参数推导位置。
6. 只启动 1 个新游戏窗口。
7. 将新窗口移动到该 slot 保存或推导出的位置和大小。
8. 将新窗口标题修正为该 slot 标题。
9. 更新该 slot 的新 `hwnd`。
10. 其它窗口不移动、不重排、不改名。

普通“排列窗口”现在默认优先按 `window_slots.json` 恢复布局。如果存在有效槽位映射：

- 不按 `hwnd` 排序。
- 不按枚举顺序排序。
- 不重新编号全部窗口。
- 不覆盖 `window_slots.json`。
- 只按 slot 恢复窗口位置和标题。

只有点击高风险操作“重新生成槽位”并二次确认后，才允许全局排序、全局排列、全局重命名并覆盖 `window_slots.json`。

全部 H5 窗口已经关闭后再执行“批量启动窗口”时，属于新窗口会话：程序不会沿用旧 `hwnd` 快照恢复，而是按当前 `window_manager_settings.json` 的窗口管理参数重新排列、重命名并生成新的 `window_slots.json`。

2026-06-10 槽位保护规则：

- 当前 profile 目标窗口数为 31 时，当前只识别到 30 个窗口，禁止“重新生成槽位”和“刷新槽位映射”，防止用不完整窗口覆盖完整槽位。
- `save_current_windows_as_slots` / `refresh_window_slots_from_current_windows` 底层也会校验 expected_count，绕过 GUI 时仍不能写入不完整槽位。
- 覆盖当前 profile 槽位文件前会先备份到 `slots/backups/`，再写 `.tmp`，最后 replace 到正式文件；写入失败不会破坏旧文件。
- “修复窗口”不再依赖当前完整映射。解析 slot 顺序为：当前 profile 槽位文件 → 当前 profile 最近备份 → legacy `window_slots.json` → 当前桌面同编号窗口 → 固定参数推导。
- 固定参数模式下可根据 slot_no、每行数量、起点、偏移和窗口宽高推导缺失 slot 坐标，例如 slot 29 可直接推导到第 4 行第 5 列。
- 修复窗口只启动 1 个新窗口并只 upsert 目标 slot，不移动其它窗口，不全量覆盖槽位文件。
- “重新生成槽位”“刷新槽位映射”“排列窗口”“修复窗口”“批量启动窗口”执行期间会禁用相关按钮，长耗时操作在 worker 线程中执行，避免 Tkinter 主线程卡死。

---

## 2.2 快速登录缓存与统一校验（2026-05-17 / 2026-05-30 更新）

当前方式一、方式二批量串行均已接入统一策略：

- 通行证弹窗坐标文件缓存：`debug_ocr/passport_dialog_pos_cache.json`。
- 缓存 key 使用浏览器真实 viewport，例如 `960x720`。
- 缓存内容包含通行证按钮、输入框、确认按钮坐标和更新时间。
- 命中缓存后只复用按钮/输入框/确认按钮坐标；必须先点击通行证按钮并轮询确认弹窗出现，确认输入框和确认按钮可定位后，才允许执行输入+确认。
- 如果缓存按钮点击后弹窗未出现，程序会清空该 viewport 缓存、重新截图模板匹配并重试；仍失败则记录“通行证弹窗未出现”，不会继续输入。
- 当前层串行 / 全部串行使用“快速提交 + 统一校验 + 失败重登”。
- 单账号运行仍保留完整校验逻辑。
- `重新次数` 用于限制统一校验和失败重登轮数；全部成功会提前结束。

2026-05-30 方式二批量策略已对齐方式一：

- 方式二 CSV 批量现在采用“快速提交 → 统一校验 → 失败重登”。
- 快速提交阶段明确区分 `already_logged_in` / `submitted` / `failed`。
- `already_logged_in` 直接计入成功，不参与统一校验，也不进入重登列表。
- `submitted` 只表示已完成账号密码登录、通行证输入和点击确认，不能直接算最终成功。
- 统一校验只校验 `submitted` 账号；失败账号只重登失败项。
- 该阶段已完成源码模式验证并提交 Git。

已验证结果：

- 已读取通行证弹窗坐标缓存。
- 已禁用旧的“点击按钮 + 固定等待 + 输入 + 确认”快路径；当前必须先验证通行证登录弹窗出现，再执行输入+确认。
- 9 个单层账号批量快速登录 + 统一校验最终全部成功。
- 最终结果：总 9，成功 9，失败 0。

安全规则：

- `qr_page` 不能判成功。
- `unknown` 不能判成功。
- 截图失败不能判成功。
- 只有明确 `logged_in` 才能成功。
- 失败账号只重登失败账号，不全量重跑。

---

## 2.3 阶段性完成状态（2026-05-17）

当前阶段已确认：

- 方式一通行证上号已验证通过。
- 方式二账号密码 + 通行证上号已验证通过。
- 前台通行证获取为复制优先、OCR 兜底；后台通行证获取为 red_bar_box 局部 OCR 多证据主路径。
- OCR 低置信度和 `c/e` 混淆不能直接接受。
- 已登录窗口不能继续 OCR。
- 二维码页不能判成功。
- `unknown` 不能判成功。
- 批量快速登录 + 统一校验 + 失败重登已验证。
- 文件级通行证弹窗坐标缓存已生效，但缓存只作为定位辅助，不能绕过弹窗出现校验。
- 旧合并 Dm chain 快路径已禁用，防止按钮未点开弹窗时继续输入。
- 固定参数排列和行数列数排列已支持。
- 单层账号和四层账号已隔离。
- 停止任务和关闭程序清理子进程已修复。
- 源码模式和 exe 模式均已测试通过。
- 已新增 `launcher-regression-guard` 防回归技能。

版本号从 `1.0.0.0` 起步；当前开发版为 `1.4.18`，下一目标版本为 `1.4.19`。后续每次发布或重要修复必须同步更新版本号。

### v1.4.19（规划中：收藏夹安全导入与账号批量参与）

- 当前只完成修复计划文档，尚未修改业务代码、尚未升版本、尚未打包正式 EXE。
- 收藏夹导入能力继续保留：首次导入和后续刷新均先在内存生成整批“更新/新增/冲突/跳过”计划，最后只备份一次、写入一次、验证一次。
- 已有完整路径唯一命中的收藏项只更新 URL 并保留原 GUID/ID；缺失路径可在已验证账号根目录内安全新增。不同分组允许同名账号，例如每层均可存在名称为 `1` 的收藏项，唯一性按规范化完整路径判断。
- 计划增加 `account_key → bookmark GUID → 完整路径` 稳定映射、根 GUID/名称/结构路径联合校验、文件哈希并发保护以及只读收藏夹检查/恢复工具。
- 直登账号首次导入默认全部参与；删除后重新导入恢复最近一次明确状态；账号管理支持多选和“当前筛选全部加入/移出”。
- 缺少 `requests` 属于启动前依赖错误，批量应停止并提示，不得静默回退 Playwright 弹出浏览器。
- 详细计划见 [docs/上号器_v1.4.19_收藏夹安全导入与账号批量参与修复计划_20260712.md](docs/上号器_v1.4.19_收藏夹安全导入与账号批量参与修复计划_20260712.md)。

### v1.4.20（收藏夹稳定根身份与现有链接同步）

- 收藏夹账号根目录改为优先按持久化 GUID 在整棵 Bookmarks 树中唯一查找；无 GUID 时才按目录名称唯一迁移。
- 旧 `roots/.../children/N` 只作兼容提示，不再从漂移下标节点临时读取新 GUID；恢复后持久化真实 GUID、名称、结构路径和父路径。
- 根目录不存在时，只在父节点明确且用户确认后，于同一整批事务创建“账号”根目录并重新绑定；取消时不写 Bookmarks。
- “刷新客户端直登地址”新增“同步现有链接到收藏夹”，直接读取账号库和 `direct_links.enc.json`，不调用 HTTP、Playwright 或重新生成 token。
- 现有链接同步会先显示更新/新增/冲突/跳过计划，确认后复用 v1.4.19 的单次备份、哈希校验、原子替换和复读验证。

### v1.4.19（收藏夹安全批量导入与账号批量参与）

- 刷新地址默认只保存本地直登链接；用户可勾选并记忆“刷新成功后同步/导入收藏夹”。
- 收藏夹改为全账号刷新完成后生成整批计划，仅读取、备份、临时写入、原子替换和复读验证各一次。
- 已有节点仅更新 URL 并保留 GUID/ID/位置/未知字段；缺失节点按规范化完整路径新增，不同分组可以同名。
- 新增 `account_key -> bookmark GUID -> 完整路径` 映射、根 GUID/名称/结构路径联合校验和处理前后文件哈希保护；任一冲突时整批不写。
- 缺少 `requests` 在第一条账号前停止，不再自动弹 Playwright；真实 HTTP 失败后仅在用户明确确认时回退。
- 新账号默认参与；账号暂时消失不再清理参与状态，重新导入恢复最近明确状态，旧 `seen_keys` 不再决定默认未参与。
- 账号管理支持多选、选中批量加入/移出、按状态+分组当前筛选批量加入/移出，每次批量只原子保存一次。
- 新增 `tools/inspect_bookmarks.py`：默认只读预览重复完整路径；恢复必须显式 `--confirm` 且先备份当前文件，不自动去重。

### v1.4.18（多倍率快捷键设置）

- 删除加速总控下方旧单快捷键整行，在 `500` 预设右侧增加“快捷键设置”按钮；弹窗固定提供四行可编辑的目标倍率、修饰键和主键。
- 四组快捷键共用一个 Windows 消息线程和一个 `GetMessageW` 消息循环，通过不同 hotkey ID 映射倍率；继续使用 `RegisterHotKey + MOD_NOREPEAT`，不增加键盘轮询。
- 点击“确定”先完成倍率、主键、组合标准化和内部重复校验，再原子替换整套注册；任一 Windows 注册失败会注销本次临时注册并恢复旧整套，成功后才保存并立即生效。取消不改变当前配置。
- 每个快捷键按目标 binding 的真实 `speed_rate` 判断：所有目标均已处于该倍率时恢复 `1.0`，否则统一切到该倍率；不同倍率快捷键可直接跳转，继续复用加速总控四种作用范围和逐 binding HWND/PID/X5Game/CDP 归属校验。
- 旧 `speed_panel_hotkey` 不再启动注册；新结构化配置字段为 `speed_rate_hotkeys`。未开发已取消的宿主侧生命周期守护，未修改 `TIMER_HOOK_SCRIPT` 核心算法，未打包正式 EXE。

### v1.4.17（实机问题修复与 UI 收口）

- 加速器快捷键改为复用加速总控当前作用范围，在设定倍率与 `1.0` 间往返切换；继续保留系统注册冲突检测、旧组合注销、清除和 `MOD_NOREPEAT`，并用非阻塞锁阻止快速连按叠加任务。
- 快捷键设置移动到加速总控第二行；“刷新地址”移动到账号配置并与“账号管理”并列；窗口管理的“游戏程序/排列方式”采用统一标签列起点。
- 关闭本批后轮询进程退出；修复时仅对同批次、同旧 PID、30 秒内刚关闭且历史 HWND 与 CDP 均已失效的 `pid_not_x5game` binding 安全转入补开，避免 PID 复用误判。
- 页面导航守护覆盖 Back、Forward、Reload、BFCache `pageshow` 和文档根节点替换，幂等恢复树形加速器并重新抑制网页原生入口，保留真实当前倍率。
- 保留 v1.4.15 的 360ms 延迟收起保护与 v1.4.16 历史 HWND/原端口完整归属校验；未修改 `TIMER_HOOK_SCRIPT` 核心倍率算法。
- 本版本只完成源码、自动化测试与文档同步，不打包正式 EXE；真实 9 窗/31 窗仍需实机复验。

### v1.4.16（旧版本批次 HWND 恢复修复）

- 修复上一个源码版本创建的客户端批次在新版本中“常规扫描 0 个”后，仍直接使用旧 PID 判断并将全部 binding 标记为 `pid_not_x5game` 的问题。
- “修复本批窗口”在常规标题扫描未匹配时，会使用历史 HWND 定向回查当前窗口 PID、进程路径、窗口矩形和 CDP endpoint，并重新执行 HWND/PID/监听端口 owner/process tree/endpoint 完整归属验证。
- 只有历史 HWND 仍有效、当前窗口进程确认为 `X5Game.exe` 且 CDP 归属为 `verified` 时，才更新 binding 的真实 PID、HWND、端口和归属状态；不会仅凭旧端口、通用标题或账号顺序猜测。
- 历史 HWND 回查保持一对一使用，重复 HWND 不允许绑定到多个账号；日志新增旧 PID、当前 PID、exe 名、端口及归属状态，便于实机定位。
- 修复成功后只保留已登录、已就绪、已排列等真实业务状态；上一次修复写入的 `pid_not_x5game` 等窗口错误状态不会重新覆盖恢复结果，无可保留业务状态时回到“客户端已就绪”。
- 保留已登录/已就绪账号不重登；未修改普通扫描、新批次准备、排列、登录、加速器和 `TIMER_HOOK_SCRIPT`。仍需对上一个版本保留的真实批次执行实机复验，未打包正式 EXE。

### v1.4.15（树形加速器误收起修复）

- 修复树形加速器在鼠标短暂触发 `mouseleave` 后，即使已经重新进入面板，旧的 360ms 收起定时器仍继续执行的问题。
- 面板重新进入时会立即取消待执行的收起任务；定时器到期前还会再次检查 `:hover`，只要鼠标实际仍在面板范围内就不会收起。
- 保留点击展开、再次点击或快捷键切换、真正移出后延迟收起、拖动、倍率步进和预设按钮等既有行为；未修改 `TIMER_HOOK_SCRIPT` 核心倍率算法。
- 本轮为源码修复和自动化回归，仍需 X5Game 实机确认人物移动与面板操作时不再误收起；未打包正式 EXE。

### v1.4.14（实机回归修复与 UI 收口）

- “待登录账号”按当前批次有效 binding 和真实登录完成状态筛选，已排列/客户端已就绪仍可手动进入游戏，已登录账号不重复执行。
- 快捷键改为组合修饰键下拉加可编辑主键下拉，支持无修饰单键、输入归一化和系统冲突提示；执行复用加速总控四种作用范围并逐 binding 隔离校验。
- 树形加速器支持拖动倍率根标签，点击与拖动分离、限制 viewport，不持久化位置，页面重建恢复左上角。
- 账号管理拆分状态筛选和动态分组筛选并组合过滤；主账号表只保留层级、收藏编号、真实窗口标题、参与全部串行和状态。
- 窗口管理压缩为两行，文案改为“自动编号标题”“禁止超宽”，旧窗口操作行及其执行入口不再创建；客户端直登批次操作保持。
- 保持并发 8、9/31 窗、缺窗原槽位、连续端口、HWND/PID/CDP 归属、窗口生命周期和 `TIMER_HOOK_SCRIPT` 核心算法。本轮未打包正式 EXE。

### v1.4.13（快捷键、账号管理与收藏夹写回修复）

- 加速器快捷键改为“修饰键 1 / 修饰键 2 / 主键”三段下拉，实时显示标准组合；重复修饰键、缺少修饰键、缺少主键、系统占用和注册失败分别提示。
- Windows 全局快捷键线程会在注册前显式创建消息队列，并记录注册、收到 `WM_HOTKEY`、前台 binding 匹配、CDP 归属阻止和回调异常；继续使用 `MOD_NOREPEAT`，修改、清除和退出均注销旧组合。
- 树形加速器根标签显示真实倍率；低于 1 倍按 0.1 增减，1 倍减为 0.9 倍，1 倍及以上按 1 增减，最低 0.1 倍；预设 1/2/5/50/500 保持独立。入口仅点击展开，鼠标移入不再展开，移出延迟收起保持不变。
- 手动“执行登录并进入游戏”始终向客户端登录 worker 传入 `auto_enter_game=true`；“自动进入游戏”复选框只控制“一键准备并登录”的连续流程。
- 删除整个高级配置区域、每层数量、旧方式二 CSV 界面和执行链、旧版兼容/后台前台登录入口与 worker；历史模式不再恢复，工作模式固定为客户端直登。
- 主账号来源统一为“刷新地址”账号库。`bookmark_path` 为空归入默认组，账号根目录直属收藏项归入单层账号，其余路径取上一级目录为动态分组；数字、字母、中文和混合账号名均支持，不再使用固定每组数量。
- 新增直登账号管理：独立保存参与状态和全局登录顺序，支持筛选、上移/下移、加入和从上号列表移除；不会修改刷新账号的 `enabled` 字段或删除账号、链接、导入文件和收藏夹。
- 收藏夹写回改为“唯一存在则更新，不存在则创建缺失目录和收藏项，多个同名路径则阻止”；空路径仍只保存本地直登链接。刷新地址的破坏性按钮改名为“移除账号”并明确资源边界。
- 修复删除高级配置后源码启动仍访问旧收藏夹目录下拉框的问题；启动现在直接加载刷新地址账号库，不再依赖已删除的高级配置控件。
- v1.4.12 已实机通过的 9 账号/31 账号并发 8、HWND/PID/CDP 归属、缺窗保槽位、连续端口、批量 5 倍/1 倍和窗口生命周期规则均作为保护范围。本轮未修改 `TIMER_HOOK_SCRIPT` 核心算法，未降低并发，未打包正式 EXE。
- 本版本源码与自动化完成后仍需实机复验快捷键触发、树形面板交互、手动进入游戏、账号管理顺序/范围和收藏夹新增写回；自动化通过不等于实机通过。

### v1.4.12（HWND 绑定排列修复与树形加速器）

- 客户端并发准备改为在同一总超时内协调等待 HWND 与 CDP；`pid<=0`、`hwnd<=0`、无效窗口、HWND/PID 不一致、CDP target 不可用或归属未验证均不再计为成功。
- GUI 准备汇总不再只信任状态字符串或 `success=true`；准备结果必须携带与 binding 完全一致的已验证 CDP 归属，排列后登录前再次验证，失效 binding 只剔除自身。
- 部分窗口失效时，排列显式保留原槽位和完整批次布局数量；第 9 槽仍使用第 9 槽坐标及 9 窗尺寸，中间缺口不补位，全量有效时保持原排列结果。
- 自研加速器改为贴边 `速` 标签和带真实分支线的树形面板，保留 `＋/－/1x/2/5/50/500`；点击或悬停展开、离开延迟收起，只有 apply 返回 `ok=true` 才更新高亮和持久化。
- 新增可清除的“加速器快捷键”，支持 Ctrl/Alt/Shift 与字母、数字、F1-F12 组合；使用 Windows `RegisterHotKey` 且禁止长按重复，只切换当前前台、精确 HWND 匹配并完成 CDP 归属验证的 X5Game binding。
- binding 已保存倍率随批次记录恢复并用于重新注入；导航重建仍由既有新文档脚本和导航守卫处理。未修改 `TIMER_HOOK_SCRIPT` 对时间函数和计时器的核心缩放算法。
- 本版本未降低并发 8、未限制窗口数、未取消排列，未改变 X5Game 可见性、最小化状态、位置/大小公式或 Z 序规则；未修改直登链接刷新和其它模块，未打包正式 EXE。
- 自动化测试不等于真实 X5Game 验收；9 账号并发 8 的 HWND 绑定/缺窗排列、树形面板视觉与快捷键冲突/前台窗口切换仍需实机验证。

### v1.4.11（加速器安全边界与控制正确性）

- 网页原生加速入口改为高置信度 DOM 删除优先、CSS 隐藏降级，并真实区分 `dom_removed`、`css_hidden`、`not_found`、`non_dom` 和 `failed`；保护 `BODY/HTML/CANVAS`、自研面板及重开按钮。
- 动态重建统一使用一个防抖 `MutationObserver`，移除重复 observer 和永久 2 秒全页面扫描；导航守卫继续在 Reload/Back/Forward 等页面事件后重新处理。
- 取消默认及旧配置触发的全局右键菜单 `preventDefault/stopPropagation` 拦截，配置模板和运行默认值同步为关闭。
- 加速总控执行前复用完整 `HWND → PID → TCP owner/进程树 → CDP endpoint` 归属校验；归属失败不调用 apply，只影响当前 binding。
- 批量加速移到后台 worker，停止后不再启动后续 binding，单个失败继续其它窗口；汇总区分成功、失败、跳过和停止。
- `apply` 返回 `ok=false` 或非确认结果时计为失败；只有真实成功才保存 `binding.speed_rate`，失败不持久化，也不覆盖 `window_status`、登录状态或其它业务状态。
- 保留 1x、2、5、50、500 倍率和选中窗口、当前批次、全部存活批次等范围；50/500 不增加确认框或风险警告。
- 本阶段未修改 timer_hook 对 `Date.now`、`performance.now`、`setTimeout`、`setInterval`、`requestAnimationFrame` 的核心缩放算法，未重做贴边面板，未集成 ChangeSpeeder。
- 自动化验证不等于实机稳定；完成 1/2/9 窗真实验收前不打包正式 EXE。

### v1.4.10（CDP 并发归属误判修复）

- 修复 31 窗、并发 8 登录时的 CDP 归属误拦截：监听端口 `owner_pid == window_pid` 时直接确认进程关系，不再读取 Toolhelp 进程快照。
- 仅当监听 PID 与窗口 PID 不同时查询父子进程树；进程快照采用进程内互斥保护、最多 3 次有限尝试和 50ms/100ms 退避，持续失败时保留异常摘要及 Windows 错误码。
- 保留 `HWND → 窗口 PID → TCP owner PID/合法进程树 → CDP endpoint` 完整校验；同 PID 也必须通过 endpoint 探测，失败时仍在 `Page.navigate` 前阻止该 binding。
- CDP 归属日志增加关系模式、快照尝试次数、快照异常、Windows 错误码、endpoint 状态和最终状态，且不输出直登 URL 或凭证。
- 自动化覆盖同 PID 快照失败、合法父子关系、非同树、快照重试/持续失败、31 binding 并发 8、endpoint 失败阻止导航及窗口生命周期不变。
- 未降低并发上限，未改变窗口可见性、位置、大小、Z 序或生命周期；未修改直登链接刷新、加速器、同步器及其他无关模块。
- 自动化验证通过不等同于实机验证；真实 31 窗、并发 8 验收完成前不打包正式 EXE，也不标记为正式稳定。

### v1.4.9（客户端常驻登录与 CDP 真实归属修复）

- 恢复 v1.3 的常驻窗口登录行为：客户端直登默认不最小化，正常登录路径不再执行 `ShowWindow` 最小化/恢复；成功、失败、超时和停止均不改变窗口可见性、位置、大小或 Z 序。
- 本地识别不再按扫描顺序、槽位、`base_port`、标题编号或历史端口范围猜测 CDP；改为验证 `HWND → 窗口 PID → TCP 监听 owner PID/直接进程树 → CDP endpoint`。
- 扫描/恢复阶段确定端口归属，发送 `Page.navigate` 前再次校验；归属缺失、未确认、不匹配、冲突或 HWND/PID 变化时只阻止对应 binding，其余账号继续。
- 本地识别批次按唯一连续槽位和账号库稳定顺序持久化 `account_key`、`refresh_account_name`、`bookmark_path`、`slot_index`、`identity_status`、`link_status`，通用窗口标题不参与账号或链接匹配。
- 最新 URL 解析支持刷新账号名与收藏夹账号名不同的情况，通过唯一 `bookmark_path` 及稳定身份字段闭环；数量不一致、重复槽位或重复路径时拒绝猜测。
- 自动化测试已通过，真实多虚拟桌面 X5Game 仍待用户实机验收；本版本未自动打包 EXE，未修改同步器、加速器/速度引擎、OCR、通行证、拍卖、YOLO、方式一/方式二稳定核心或窗口排列算法。

### v1.4.8（账号生命周期与登录闭环修复）

- 恢复历史批次或识别本地客户端后，先按稳定账号键或明确槽位解析账号身份，再为每个 binding 注入本地受保护链接库中的最新 URL；窗口显示标题不再参与链接匹配。
- 数量不一致、槽位重复、身份冲突、链接缺失和 CDP 不可用分别保留真实状态，缺少完整链接时不会继续以 `/` 执行登录。
- CSV 第四列 `bookmark_path` 为空时生成到 `上号器数据\直登链接`，非空时生成到 `上号器数据\分组\<完整路径镜像>`；路径迁移会在新文件校验成功后精准清理旧生成文件。
- 删除刷新账号只清理程序账号记录、直登链接记录、精确归属的 `.url`、内存缓存和批次关联；不会修改用户 CSV、浏览器收藏夹、备份、日志或历史汇总。
- “刷新地址”弹窗按主窗口所在显示器工作区居中，支持副屏负坐标和屏幕边缘夹取，不依赖固定分辨率或 DPI。
- 本版本未修改加速器、同步器、OCR、通行证、方式一收藏夹直登、拍卖、YOLO 或窗口槽位稳定核心，且未自动打包 EXE。

### v1.4.7（客户端批次最新链接与稳定性加固）

- 新批次准备、追加准备、单账号直登、串行直登和已有批次执行登录前，都会重新解析本地受保护链接库中的最新 URL。
- 同一账号存在多个不同候选链接时拒绝注入并保留收藏夹原链接；本地缺失时同样保持原流程不变。
- 最新链接唯一命中时同步当前内存 binding 和批次 binding 并持久化；过期链接给出计数提示但仍按原规则允许尝试。
- 登录阶段只最小化当前 binding 对应的窗口，成功、失败或停止后均恢复；原本已最小化或不可见的窗口不会被强制恢复。
- 并发准备保持最大 8 个 worker，并增加共享进程启动节流；停止信号置位后排队项不再启动。
- 31 窗口纯模拟覆盖连续端口、单窗失败继续和停止后不启动后续项；没有枚举、接管、排列或关闭未知 X5Game。
- 本版本未修改加速器、同步器、OCR、通行证、方式一收藏夹直登或窗口槽位核心。

### v1.4.6（刷新停止、数据保护与弹窗收口）

- 纯 HTTP 刷新拆分连接超时和读取无响应超时，并在每个请求步骤前后检查停止信号。
- 停止后不再重试当前账号、不回退 Playwright、不启动下一个账号；GUI 明确显示“停止中/已停止”。
- `accounts.enc.json` 和 `direct_links.enc.json` 改为 `schema_version=2` 的 Windows DPAPI 受保护封装，账号密码和完整直登 URL 不再以普通 JSON 明文保存。
- 首次读取旧明文文件时先创建受保护迁移备份，再原子替换源文件；保护或迁移失败时保留原文件并继续兼容读取。
- 源码模式继续使用项目内 `上号器数据`；打包模式默认使用 `%APPDATA%\DouluoH5Launcher\refresh_data`，可用 `H5_LAUNCHER_REFRESH_DATA_DIR` 覆盖。
- 旧刷新目录按文件更新时间迁移，目标文件更新时不覆盖，替换旧目标前先备份。
- 刷新弹窗显示当前浏览器/profile、备份位置，支持打开备份和 `.url` 目录；收藏夹路径编辑时即时预览唯一命中、缺失或冲突。
- 刷新运行中关闭弹窗会先触发停止，等待线程结束后再关闭，避免后台更新已销毁控件。

### v1.4.5（收藏夹精准安全写回）

- CSV 第四列 `bookmark_path` 支持按完整层级精准定位收藏项，兼容省略已选根目录以及 Windows/Unix 路径分隔符。
- 写回要求唯一命中；路径不存在或存在冲突时只保留已生成的本地链接，不误改同名收藏项。
- 写回前每批只创建一次原文件备份，临时 JSON 复读校验通过后再原子替换，并同步 Chromium Bookmarks checksum。
- Edge 或 Chrome 正在运行时阻止真实 profile 写回，不强制关闭浏览器。
- 写回结果分别统计本地成功、收藏夹成功、收藏夹失败和收藏夹跳过；普通日志和汇总不记录完整直登 URL。
- GUI 写回后立即同步主账号内存 URL，并在唯一匹配时更新当前客户端批次 `login_url`；冲突时保留原值。
- CLI 默认保持收藏夹 dry-run；只有显式提供 Bookmarks 文件、浏览器、profile、根节点路径并传入 `--write-bookmarks` 时才允许真实写回。
- 已用真实 Bookmarks 临时副本按 1 条、9 条顺序验证，9 条 CSV 路径均唯一命中；真实 profile 写回仍受浏览器运行状态保护。

### v1.4.4（刷新地址 GUI 接入 HTTP 优先）

- 现有“刷新地址”弹窗默认使用 `auto` 刷新策略：HTTP 成功后直接保存，失败才回退 Playwright。
- GUI 与正式 CLI 共用同一层刷新策略编排，继续复用 `DirectLinkRefreshService` 更新链接库、汇总文件和 `.url`。
- 刷新仍在后台线程执行，停止后不再启动后续账号或 Playwright 回退。
- GUI 状态改为客户可读文本，区分本地刷新成功、收藏夹成功和收藏夹未写回。
- 本地链接成功但收藏夹尚未写回时统计为“未写回”，不计入整体失败或收藏夹失败。
- 保留 Playwright 回退，不改方式一收藏夹直登、OCR、窗口管理、客户端批次、CDP、同步器和加速器。

### v1.4.3（正式刷新流程接入 HTTP 优先）

- `tools/refresh_client_direct_links.py` 已正式接入纯 HTTP 登录，不再只是独立验证脚本。
- 默认 `--login-mode auto`：优先纯 HTTP；HTTP 失败时自动回退现有 Playwright 登录流程。
- 支持 `--login-mode http` 仅走 HTTP，以及 `--login-mode playwright` 强制沿用原浏览器流程。
- HTTP 成功后继续复用 `DirectLinkRefreshService`，更新 `direct_links.enc.json`、刷新账号状态、生成对应 `.url` 和脱敏汇总。
- 正式单账号命令：`python .\tools\refresh_client_direct_links.py --import-file ".\上号器数据\存钻小号.csv" --limit 1 --login-mode auto`。
- 本次仍不修改 GUI；现有 Playwright 能力保留为自动回退和手动强制模式。

### v1.4.2（CSV 单账号 HTTP 验证入口）

- 新增 `tools/run_http_direct_login_from_csv.py`，可从本地 CSV/TXT/XLSX 账号文件选择账号并复用纯 HTTP 登录核心。
- 默认只处理第一条有效账号；可用 `--name` 精确选择账号，或用 `--limit` 控制最多处理条数。
- 不修改原账号文件，不打印账号密码，不在普通日志中输出完整 token、sign 或直登 URL。
- 单账号验证命令：`python .\tools\run_http_direct_login_from_csv.py --csv ".\上号器数据\存钻小号.csv" --limit 1`。
- 已使用 `上号器数据\存钻小号.csv` 第一条真实账号完成验证，成功获得 `uid/uname/token/time/sign` 并构造 `dldl.50pk.com/login.php` 客户端直登地址。
- 该入口仅用于本机验证，不修改 GUI，也不替换现有 Playwright 刷新流程。

### v1.4.1（纯 HTTP 刷新 POC）

- 新增 `tools/probe_http_direct_login.py`，可脱敏分析登录 HAR，并完成纯 HTTP 账号密码登录。
- 已还原 37 SDK 模块 66 配置解码、`API_KEY` 提取和动态签名算法；签名不会复用 HAR 中的旧值。
- HTTP 链路固定为：访问 37 登录入口 → 获取当前 `bundle.js` → 调用 `h5sdk/active` → 调用 `h5sdk/login` → 提取 `token/time/sign/uid/uname`。
- 使用全新 Session，不复用 HAR 中的旧 Cookie、账号或密码；局部 HAR 缺少 bundle/active 时会从完整研究 HAR 补齐请求模板。
- 已使用真实研究 HAR 复算 `h5sdk/active` 与 `h5sdk/login` 签名并保持一致；真实账号 HTTP 登录需在本机交互运行后确认，输出仅保留字段长度。
- 测试账号和密码仅在控制台交互输入，不写入代码、日志或本地文件。
- 该 POC 不修改 GUI，不替换现有 Playwright 刷新流程，也不写入账号库和直登链接库。
- 仅分析 HAR：`python .\tools\probe_http_direct_login.py --analyze-only`
- 发起纯 HTTP 测试：`python .\tools\probe_http_direct_login.py`，账号密码在控制台交互输入且不保存。

### v1.4.0（刷新地址 / 直登链接刷新器）

- 新增“刷新地址 / 直登链接刷新器”功能。
- 账号密码仅用于换取新的客户端直登链接，不再作为前端登录进游戏主流程。
- 新增登录渠道配置，账号文件不再需要 url 字段。
- 新增本地账号库和本地直登链接库。
- 支持导入账号文件、剪贴板导入、手动维护账号。
- 支持刷新本地链接和刷新收藏夹地址两种模式。
- 支持生成 `.url` 文件作为本地备份。
- 客户端直登模式优先从本地直登链接库读取最新链接。
- 日志脱敏，不输出账号密码和完整 token/sign。

### v1.3.2（账号密码刷新客户端直登链接）

- 新增 `tools/refresh_client_direct_links.py`，可从 CSV 账号密码登录捕获 `h5sdk/login` 返回的直登凭证并刷新对应 `.url` 文件。
- 输出目录默认为 `output/直登链接/`，按 CSV 的 `name` 字段生成或覆盖同名 `.url`，同时更新不含完整 token/sign 的 `summary.csv`。
- 不修改浏览器收藏夹，不接入正式 GUI 登录流程，日志继续避免输出完整账号密码、token 和 sign。

### v1.3.1（客户端批次窗口状态修复）

- 识别本地客户端恢复历史批次时，未被当前扫描匹配到的历史绑定会标记为“未找到”，不能继续显示“客户端已就绪”。
- 修复本批窗口会重新扫描当前 X5Game 窗口并校验 pid、进程名、CDP 端口和 hwnd；日志会显示当前扫描数、历史绑定数、就绪数和异常数。

### v1.3.0（发布持久化 + 加速总控稳定版）

- 用户运行配置统一保存到 `%APPDATA%\DouluoH5Launcher`，支持新 zip 解压到新目录后继续读取旧配置。
- `automation_settings.json` 由模板补缺字段，不覆盖用户已有路径、端口、窗口布局和加速器配置。
- `client_direct_sessions.json` 持久化保存，关闭上号器后重新打开可恢复旧批次并继续控制 CDP 可用窗口。
- 新增加速总控，支持当前批次、选中窗口、全部存活批次、CDP 可用窗口批量应用倍率。
- Back / Forward / Reload 后通过新文档预注入恢复自定义变速器，并继续隐藏原网页加速浮层和火箭入口。
- 修复本批窗口只修复窗口绑定，不覆盖客户端登录成功、已进入游戏等业务状态。
- `speed-hack-panel` 按钮改为 grid gap 布局，应用 / 重置 / 50 / 500 间距更稳定。
- 方式一前台辅助降级为旧版兼容入口，客户默认主入口聚焦客户端直登。
- 客户发布包不包含用户真实配置、批次记录、日志、debug 数据、token 或 sign。

### v1.2.0（客户端直登自定义变速器）

- 客户端直登模式新增“自定义变速器 / 加速面板”。
- 自动隐藏网页原始“请选择加速倍率”浮层，不删除 canvas 或游戏主容器。
- 通过 CDP `Runtime.evaluate` 注入固定 id：`speed-hack-panel`。
- 自定义面板默认左上角、深色半透明、小尺寸、支持拖动。
- 面板按钮包含应用 / 重置 / 50 / 500，右上角 X 只隐藏面板。
- 默认倍率保持 `1.0`，不会进入游戏后自动加速。
- 默认使用 `speed_engine="timer_hook"` 自定义时间加速脚本，带防重复 hook 保护。
- 默认 `speed_hook_stage="after_game_ready"`，避免在 `Page.navigate` 后过早 hook 时间函数。
- 高级配置新增：替换网页加速浮层、显示自定义变速器、默认倍率、面板位置。
- 继续保持 URL / token / sign / cookie / IMEI / uid / session 脱敏。

### v1.1.2（客户端直登 UI 收口）

- 客户端直登 UI 收口。
- 工作模式切换：客户端直登模式 / 前台辅助模式。
- 读取收藏夹 / 账号配置与运行区分离。
- 客户端直登支持已有存活批次时创建新批次。
- 新批次自动避开旧批次端口。
- 客户端直登接入受控并发，默认 1，最大 8。
- 排列窗口和重命名窗口保持串行。
- 修复本批窗口支持 `pid_missing` 补开。
- `pid_missing` 补开后自动移动到对应槽位。
- 修复本批窗口不再覆盖“客户端登录成功”等业务状态。
- 新增登录范围：待登录账号 / 选中账号 / 失败账号 / 全部账号。
- 默认登录范围为“待登录账号”，避免全批重登。
- 日志默认隐藏。
- URL / token / sign 等继续脱敏。

### v1.1.1（客户端直登 UI 调整）

- 工作模式顶层切换。
- 客户端直登 / 前台辅助运行区隔离。
- 窗口管理按模式隐藏旧启动项。
- 读取收藏夹作为公共账号来源。
- 失效批次自动清理。
- 删除当前批次 / 清理失效批次。
- 一键准备并登录自动端口推荐与 `auto_enter_game` 修复。
- URL 脱敏显示。

---

## 2.5 最近修复（2026-05-12/13/14）

| 问题 | 修复 |
|------|------|
| `subprocess.Popen` monkey-patch 导致 asyncio 崩溃 | function→class 继承（`_NoConsolePopen`），Playwright 导入前恢复原始 Popen |
| 重试时相同通行证跳过完整流程 | 删除跳过逻辑，重试始终走完整浏览器流程 |
| exe 启动有黑框 | 切换 `--noconsole` 引导器 |
| 登录程序窗口状态判断不稳定 | 新增 `detect_login_page_state` 图像特征检测 |
| 二维码页 OCR 被 QR 码干扰 | 确认 qr_page 后优先用底部文字区域 OCR |
| 日志路径不统一 | 新增 `project_root()`，exe 模式日志也落到项目根 `logs/` |
| 方式一失败状态不更新 | `run_game_flow` retry=1 缺少 `raise RuntimeError` |
| 方式二 finally 固定 sleep(3) | 已登录账号不打开浏览器也等3s → 有浏览器才等2s |
| CSV 耗时列偏移 | `values[-1]` 指向 timing 列 → 改为显式 `values[6]` |
| OCR hex 字符混淆 c↔0/e | 通行证获取已改为复制优先；OCR 兜底遇到低置信度或 c/e 竞争必须失败 |
| 停止任务后仍可能继续移动鼠标 | 已修复：停止按钮会强制终止当前账号子进程、清理 `dm_click_helper.py` 和 `chromium.exe` |
| 关闭 GUI 后仍可能残留子进程 | 已修复：窗口关闭会先执行停止和清理，并避免 `_drain_ui_queue` 继续 after 回调 |

## 3. 当前架构

```
┌─────────────────────────────────────────────────┐
│  64-bit Python (主进程)                          │
│  ├── Tkinter GUI (LauncherApp)                  │
│  ├── Playwright (浏览器管理)                      │
│  ├── Tesseract OCR (全图通行证提取)               │
│  ├── OpenCV (模板匹配 + 视觉定位)                  │
│  └── BitBlt (后台截图)                           │
│       │                                          │
│       ▼ 写 browser_pos.json                      │
│  32-bit Python (Dm 子进程)                       │
│  ├── dm.MoveTo + LeftDown/LeftUp (前台点击)       │
│  └── clip.exe + dm.KeyDown(Ctrl+V) (剪贴板输入)   │
│                                                  │
│  前台串行：Popen 子进程逐账号隔离 Playwright        │
└─────────────────────────────────────────────────┘
```

---

## 4. 对象关系

- 收藏编号只用于读取收藏夹链接
- 游戏窗口号 = 收藏编号 + 层级偏移量（第一层+0, 第二层+8, 第三层+16, 第四层+24）
- 登录程序窗口标题如 `斗罗大陆H5-9-伊导科技`，按游戏窗口号匹配
- 二维码和通行证只存在于登录程序窗口（WindowsForms 桌面应用）
- 浏览器游戏页面只有游戏 canvas 和 UI 按钮

---

## 5. 两种上号方式

### 方式一：收藏夹链接 + 通行证上号（✅ 已开发，当前主流程）

- 从浏览器收藏夹读取游戏入口链接
- 前台辅助模式从登录程序窗口优先复制 8 位通行证，复制失败才进入 OCR 兜底
- 后台登录模式禁止复制/读取链路，直接使用 red_bar_box 局部 OCR 多证据提取通行证
- 支持单账号、当前层串行、全部串行

### 方式二：CSV 配置文件 + 账号密码 + 通行证上号（✅ 已开发）

- 通过 CSV 文件配置账号（`name,url,username,password`）
- 仍然需要先从登录程序窗口获取通行证，不是只用账号密码
- 方式二仍运行在前台辅助链路下，通行证获取遵循复制优先、OCR 兜底
- password 不打印到日志，GUI 仅显示"已填写/未填写"
- 支持单账号和串行批量

### 通行证获取优先级

前台辅助模式：

1. 第一优先级：双击选中登录窗口中的 8 位通行证，`Ctrl+C` 复制，读取剪贴板。
2. 第二优先级：复制失败后才进入 OCR 兜底。

后台登录模式：

1. 后台截图登录窗口。
2. 已进入游戏则跳过 OCR。
3. 二维码登录页直接使用 red_bar_box 局部 OCR 多证据识别。
4. 失败则保存现场并报错，不尝试 WM_GETTEXT、UIA、后台 Ctrl+C 或剪贴板。

前台 OCR 不再作为第一优先级。原因是 OCR 存在单字符误识别风险，例如真实值 `4425cbaa` 可能被识别为 `4425ebaa`，错误位置为 `c` 被识别为 `e`。前台复制成功时直接使用复制结果；复制失败时才进入 OCR。后台使用已经 live 验证成功的 red_bar_box 局部 OCR 强证据路径。OCR 低置信度时不得继续输入通行证，存在 `c/e` 等混淆时必须失败或进入人工处理，不能假成功。

---

## 6. 运行方式

详见 [RUN_MODE.md](RUN_MODE.md)。

```powershell
cd <项目路径>
python main.py
```

GUI 按钮：
- **单账号运行** — 运行下拉框选中的账号
- **当前层串行** — 只运行当前层级下拉框选中的分组，不受“全部串行分组设置”影响
- **全部串行** — 只运行 `include_in_all=true` 的已启用分组；层级不是 `全部` 时会先阻止并提示语义差异

当前层串行 / 全部串行使用“快速提交 + 统一校验 + 失败重登”：

- 登录前已是 `logged_in` 的账号直接标记“已登录/成功”，不进入待复核。
- 已输入通行证并点击确认的账号标记“待复核”，后续统一校验。
- 提交前失败的账号进入失败/重登列表。
- 单账号运行仍保留完整校验逻辑。

### 打包发布

```powershell
.\scripts\build_exe.bat
```

输出：`dist/Launcher/上号器.exe`

说明：内部打包目录使用英文 `Launcher`，避免 Windows bat/cmd 对中文目录名处理不稳定；最终用户看到的 exe 名称仍固定为 `上号器.exe`。

详见 [BUILD.md](BUILD.md)。

---

## 7. 项目结构

详见 [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)。

核心文件：
- [main.py](main.py) — 程序入口
- [douluo_launcher/gui.py](douluo_launcher/gui.py) — Tkinter GUI
- [douluo_launcher/automation.py](douluo_launcher/automation.py) — 账号运行流程
- [douluo_launcher/config.py](douluo_launcher/config.py) — 配置和收藏夹读取
- [douluo_launcher/dm_client.py](douluo_launcher/dm_client.py) — 窗口管理和后台截图
- [douluo_launcher/window_manager.py](douluo_launcher/window_manager.py) — 登录窗口批量启动、识别、排列、关闭、重命名
- [douluo_launcher/window_manager_settings.py](douluo_launcher/window_manager_settings.py) — 窗口管理参数记忆
- [dm_click_helper.py](dm_click_helper.py) — 32 位 Dm 点击/输入脚本
- [automation_settings.json](automation_settings.json) — 自动化参数

**运行必要目录**：
- `debug_ocr/` — 运行必须（含 `template_passport_btn.png`、`browser_pos.json`）
- `logs/` — 日志目录（运行时自动创建）

**临时目录**（可清理）：
- `debug_ocr/_tmp/` — 临时截图（自动清理）
- `_cleanup_pending/` — 归档待清理文件，确认无问题后可删除

---

## 8. 依赖环境

```text
主 Python: 3.14.2, 64 位（Playwright + GUI）
32-bit Python: 3.14.4, py -3.14-32（Dm 插件）
Playwright: 1.59.0（Chromium）
Tesseract OCR: 需独立安装
OpenCV: 4.13.0（模板匹配 + 视觉定位）
大漠: 7.2607（仅限 32 位，无绑定前台模式）
```

---

## 9. 文档索引

按阅读顺序：

1. 本文档 — 项目概述
2. [MILESTONE_FRONTEND_SERIAL.md](MILESTONE_FRONTEND_SERIAL.md) — 当前里程碑
3. [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) — 文件结构
4. [RUN_MODE.md](RUN_MODE.md) — 运行模式说明
5. [GUI_STATUS_FLOW.md](GUI_STATUS_FLOW.md) — GUI 状态流转
6. [OCR_SUCCESS.md](OCR_SUCCESS.md) — OCR 方案
7. [CLICK_SOLUTION.md](CLICK_SOLUTION.md) — 点击方案
8. [BUILD.md](BUILD.md) — 打包发布
9. [DEBUG_IMAGE_POLICY.md](DEBUG_IMAGE_POLICY.md) — 截图管理策略
10. [LOG_POLICY.md](LOG_POLICY.md) — 日志策略
11. [CURRENT_ISSUES.md](CURRENT_ISSUES.md) — 当前问题和限制
12. [NEXT_STEPS.md](NEXT_STEPS.md) — 后续方向
13. [KNOWN_BUGS.md](KNOWN_BUGS.md) — 重复踩坑记录
14. [ERROR_HISTORY.md](ERROR_HISTORY.md) — 历史回归问题台账，记录禁止复发的问题
15. [REGRESSION_TESTS.md](REGRESSION_TESTS.md) — 防回归测试映射和打包前检查命令
16. [DEVELOPMENT_RULES.md](DEVELOPMENT_RULES.md) — 项目开发规则
17. [DOC_UPDATE_PROMPT.md](DOC_UPDATE_PROMPT.md) — 文档整理通用指令（每次整理文档前必读）
18. [BUILD_RELEASE_PROMPT.md](BUILD_RELEASE_PROMPT.md) — 打包发布通用指令（每次打包前必读）
19. [docs/WINDOW_MANAGER_AND_PASSPORT_MILESTONE.md](docs/WINDOW_MANAGER_AND_PASSPORT_MILESTONE.md) — 窗口管理与通行证阶段总结
20. [docs/上号器_v1.4.19_收藏夹安全导入与账号批量参与修复计划_20260712.md](docs/上号器_v1.4.19_收藏夹安全导入与账号批量参与修复计划_20260712.md) — v1.4.19 待授权修复计划

---

## 9.1 防回归检查

以后每修一个 bug，必须在 [ERROR_HISTORY.md](ERROR_HISTORY.md) 登记历史问题，并在 [REGRESSION_TESTS.md](REGRESSION_TESTS.md) 记录对应测试。没有回归测试的修复，不算完成。

提交、push、打包前必须执行：

```powershell
python -m compileall -q douluo_launcher main.py
python -m unittest discover -s tests -v
```

如果回归测试失败，禁止提交、禁止 push、禁止打包。

---

## 10. 禁止事项

- 不要回退到二维码定位 + 裁剪 OCR 方案
- 不要从浏览器页面 OCR 通行证
- 不要用 Dm 7.2607 BindWindow（全模式崩溃）
- 不要使用 Playwright/CDP/PostMessage/SendMessage 点击 canvas
- 不要开启真并发（前台模式下不支持）
- 文档任务只允许改文档，不要顺手改业务代码
- 打包必须单独确认，不得在普通开发任务中自动打包
- 停止任务不能只设置 `stop_event`，必须同步清理当前账号子进程、`dm_click_helper.py` 和本次 Playwright/Chromium 相关进程
- 禁止误杀所有 `python.exe`，只能清理本项目记录的子进程或命令行明确包含 `dm_click_helper.py` 的进程

### v1.4.20 正式构建状态（2026-07-12）

- 已修复冻结态根目录越界：EXE 不再从 `dist` 上溯开发仓库，也不把 cwd 或其祖先作为旧数据迁移源。
- 源码模式仍使用项目根 `上号器数据`；EXE 用户数据仍写入 `%APPDATA%\DouluoH5Launcher`，仅允许从发布目录内部兼容位置和当前用户旧 APPDATA 安全迁移。
- 缺少研究 HAR 时，`auto/http` 会在启动 Playwright 前停止；只有依赖完整且真实 HTTP 登录失败后，才允许用户明确确认回退。
- 废弃构建 SHA-256 `7E9CFE70...0984A` 不得交付。最终重新构建 EXE SHA-256 为 `048345665E198BA9E694D5A6D29A63A9DB12327C6367990A162E3B306349B2CB`。
- 自动化测试、源码 6 秒烟雾、两组隔离 EXE 12 秒启动和发布包静态安全扫描已通过。多倍率快捷键弹窗源码定位已由用户确认居中。
- 正式 EXE 下真实 9/31 窗、快捷键注册触发、刷新地址/账号管理/收藏夹写入仍待用户最终实机验证；生成 EXE 不等于全部发布验证通过。
