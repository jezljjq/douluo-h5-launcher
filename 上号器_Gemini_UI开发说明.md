# 斗罗大陆 H5 上号器 UI 开发说明

> 目标：让 Gemini 负责 **UI 界面开发**，Codex 后续负责 **后端自动化接入**。  
> 当前阶段只做界面、布局、状态展示、配置入口、接口预留，不实现真实自动化逻辑。

---

## 1. 项目定位

“斗罗大陆 H5 上号器”是一个用于批量打开游戏账号、识别通行证、进入游戏并完成通行证登录的桌面工具。

核心流程包括：

1. 从浏览器收藏夹读取账号链接；
2. 批量打开指定层级账号；
3. 识别二维码登录页底部的“本次通行证”；
4. 打开正式游戏页面；
5. 自动关闭公告；
6. 点击游戏内“通行证”按钮；
7. 输入识别到的通行证；
8. 点击确认；
9. 校验是否登录成功；
10. 支持窗口排列、窗口编号、失败记录、批量重试。

---

## 2. Gemini 开发边界

### 2.1 Gemini 只负责 UI

Gemini 当前只允许开发以下内容：

- PySide6 界面；
- 页面布局；
- 按钮；
- 表格；
- 配置面板；
- 状态栏；
- 日志区域；
- 空接口；
- signal / slot 占位；
- 模拟状态展示；
- UI 样式优化。

### 2.2 Gemini 不负责真实后端逻辑

不要实现以下内容：

- OCR 识别逻辑；
- Playwright / Selenium 逻辑；
- 大漠后台点击逻辑；
- 真实窗口绑定逻辑；
- 真实收藏夹解析逻辑；
- 真实浏览器控制逻辑；
- 真实账号登录逻辑；
- 真实通行证输入逻辑；
- 真实成功校验逻辑。

### 2.3 严禁伪成功

UI 不能伪造以下内容：

- 登录成功；
- OCR 成功；
- 窗口绑定成功；
- 通行证输入成功；
- 校验成功；
- 账号执行完成。

前端只能显示后端真实传入的状态。

---

## 3. 总体 UI 风格

要求：

- 中文界面；
- 专业工具软件风格；
- 清晰、稳定、易操作；
- 不要花哨；
- 不要网页化；
- 不要过度设计；
- 不要添加无关高级配置；
- 左侧导航 + 右侧主内容；
- 顶部显示整体运行状态；
- 底部显示日志和任务进度。

推荐状态颜色：

| 状态 | 颜色建议 |
|---|---|
| 等待 | 灰色 |
| 执行中 | 蓝色 |
| 成功 | 绿色 |
| 失败 | 红色 |
| 跳过 | 橙色 |

---

## 4. 主窗口布局

推荐主窗口结构：

```text
┌──────────────────────────────────────────────┐
│ 顶部状态栏：项目名称 / 模式 / 浏览器 / 成功失败统计 │
├───────────────┬──────────────────────────────┤
│ 左侧导航栏     │ 右侧主内容区域                 │
│               │                              │
│ 总控台         │ 当前页面内容                   │
│ 账号任务       │                              │
│ 窗口管理       │                              │
│ OCR 设置       │                              │
│ 运行日志       │                              │
│ 系统配置       │                              │
├───────────────┴──────────────────────────────┤
│ 底部日志 / 当前任务进度 / 状态提示             │
└──────────────────────────────────────────────┘
```

---

## 5. 顶部状态栏

顶部显示当前核心状态：

- 项目名称：斗罗大陆 H5 上号器；
- 当前运行模式：方式一 / 方式二；
- 当前浏览器：Edge / Chrome；
- 当前 Profile；
- 已选择账号数量；
- 正在运行任务数；
- 成功数量；
- 失败数量；
- 当前 OCR 引擎；
- 当前状态：空闲 / 执行中 / 已停止 / 已完成。

示例：

```text
斗罗大陆 H5 上号器 | 模式：方式一 | 浏览器：Edge | 已选账号：31 | 成功：28 | 失败：3 | 状态：执行中
```

---

## 6. 左侧导航栏

左侧导航栏包含以下页面：

1. 总控台；
2. 账号任务；
3. 窗口管理；
4. OCR 设置；
5. 运行日志；
6. 系统配置。

不要把导航做得太复杂。

---

# 7. 页面一：总控台

总控台是用户最常用的页面。

---

## 7.1 运行模式选择

提供两个模式：

### 方式一：收藏夹直登

说明：

```text
适用于已经保存到浏览器收藏夹的账号链接。
流程：打开收藏夹账号 → OCR 通行证 → 打开游戏页 → 公告关闭 → 输入通行证 → 校验登录。
```

### 方式二：登录程序 + 账号密码 + 游戏页

说明：

```text
适用于需要账号密码登录的流程。
流程：打开登录程序 → OCR 通行证 → 打开游戏页 → 输入账号密码 → 进入游戏 → 复用通行证流程。
```

---

## 7.2 核心操作按钮

主按钮包括：

- 开始执行；
- 停止任务；
- 暂停任务；
- 继续任务；
- 只重试失败账号；
- 校验当前窗口；
- 打开日志目录。

按钮状态要求：

| 场景 | 按钮状态 |
|---|---|
| 未选择账号 | “开始执行”禁用 |
| 正在运行 | “开始执行”禁用，“停止任务”启用 |
| 已停止 | 根据实际状态显示“继续任务” |
| 失败数量为 0 | “只重试失败账号”禁用 |

---

## 7.3 执行进度

显示内容：

- 总账号数；
- 已完成；
- 成功；
- 失败；
- 跳过；
- 当前正在处理账号；
- 当前步骤。

示例：

```text
总数：31
已完成：18
成功：16
失败：2
当前账号：第二层-5
当前步骤：正在识别通行证
```

进度条：

- 总进度；
- 当前账号步骤进度。

---

# 8. 页面二：账号任务

该页面用于选择账号、层级、执行范围。

---

## 8.1 账号来源

顶部提供账号来源选择：

- 浏览器收藏夹；
- CSV 文件；
- 手动导入。

当前阶段只做 UI 展示，不需要真实解析收藏夹或 CSV。

---

## 8.2 收藏夹账号树

显示结构示例：

```text
账号
├── 第一层
│   ├── 1
│   ├── 2
│   └── ...
├── 第二层
├── 第三层
└── 第四层
```

每个账号前面有 checkbox。

支持功能：

- 全选；
- 反选；
- 清空选择；
- 只选当前层；
- 按层级筛选；
- 搜索账号；
- 显示已选数量。

---

## 8.3 账号任务表格

表格字段：

| 字段 | 说明 |
|---|---|
| 序号 | UI 序号 |
| 层级 | 第一层 / 第二层 / 第三层 / 第四层 |
| 账号编号 | 1 / 2 / 3 |
| 账号名称 | 收藏夹名称 |
| 账号链接 | 可省略显示或折叠 |
| 通行证 | OCR 识别结果 |
| 当前状态 | 等待 / 执行中 / 成功 / 失败 |
| 失败原因 | OCR失败 / 找不到窗口 / 校验失败 |
| 重试次数 | 当前重试次数 |
| 操作 | 单独执行 / 跳过 / 查看日志 |

注意：

- 状态必须由后端传入；
- UI 不允许自己判断成功；
- UI 不允许写死账号数量；
- UI 不允许写死层级数量。

---

# 9. 页面三：窗口管理

用于管理游戏窗口排列、窗口编号、执行窗口选择。

---

## 9.1 窗口扫描区域

按钮：

- 扫描当前窗口；
- 刷新窗口列表；
- 绑定选中窗口；
- 清空窗口列表；
- 窗口标题编号；
- 恢复窗口排列。

窗口表格字段：

| 字段 | 说明 |
|---|---|
| 选择 | checkbox |
| 序号 | UI 序号 |
| hwnd | 窗口句柄 |
| 标题 | 当前窗口标题 |
| 绑定账号 | 对应账号 |
| 位置 | x, y |
| 尺寸 | width, height |
| 状态 | 未绑定 / 已绑定 / 执行中 / 异常 |

---

## 9.2 排列方式一：固定参数排列

字段：

- 打开数量；
- 每行数量；
- 起点 X；
- 起点 Y；
- 窗口宽度；
- 窗口高度；
- 横向偏移；
- 纵向偏移；
- 应用到选中窗口；
- 应用到全部窗口。

当前环境临时参数可作为默认示例，但不能写死到逻辑中：

```text
打开数量：31
每行数量：8
起点 X：250
起点 Y：0
横向偏移：320
纵向偏移：525
```

必须标注：

```text
以上为当前环境临时参数，后续应通过配置文件或界面参数传入，不允许写死。
```

---

## 9.3 排列方式二：行列自适应排列

字段：

- 行数；
- 列数；
- 屏幕选择；
- 是否保持比例；
- 是否自动缩放；
- 是否排列后自动编号标题。

---

## 9.4 窗口配置原则

UI 必须支持配置化：

- 不写死窗口大小；
- 不写死窗口数量；
- 不写死屏幕分辨率；
- 不写死缩放比例；
- 不写死坐标；
- 不写死标题模板；
- 不写死 hwnd；
- 所有参数都通过配置项传给后端。

---

# 10. 页面四：OCR 设置

用于展示和配置通行证 OCR。

---

## 10.1 OCR 基础设置

字段：

- OCR 引擎：
  - Tesseract；
  - PaddleOCR；
  - 后续扩展；
- OCR 识别目标：
  - 本次通行证；
  - 按钮文字；
  - 公告文字；
  - 提示文字；
- 识别语言；
- 超时时间；
- 重试次数；
- 最小置信度。

---

## 10.2 通行证识别设置

重点显示：

```text
识别目标文本：本次通行证：
```

说明：

```text
二维码登录页底部的“本次通行证”文字在 canvas 中，不能直接通过 DOM 读取，需要 OCR 识别。
```

配置项：

- 是否启用固定 ROI；
- 是否启用动态定位；
- ROI 区域预览；
- 截图预览；
- OCR 结果预览；
- 置信度显示；
- 保存失败截图；
- 打开 debug_ocr 目录。

按钮：

- 测试 OCR；
- 预览截图；
- 清理 OCR 临时文件；
- 打开 OCR 日志。

---

# 11. 页面五：运行日志

日志页面只负责展示后端日志。

---

## 11.1 日志过滤

支持按级别过滤：

- INFO；
- WARN；
- ERROR；
- DEBUG。

支持按账号过滤。

---

## 11.2 日志操作

按钮：

- 清空前端显示；
- 打开日志文件；
- 导出本次日志；
- 复制选中日志。

---

## 11.3 日志格式

日志格式示例：

```text
[15:31:22] [INFO] [第一层-1] 开始处理账号
[15:31:24] [INFO] [第一层-1] OCR识别通行证：123456
[15:31:27] [ERROR] [第一层-1] 校验失败：未检测到登录成功状态
```

注意：

- 前端日志只显示后端真实事件；
- 不要在 UI 中写死“成功”；
- 不要用模拟日志代替真实运行日志；
- 调试日志和用户日志要分级显示。

---

# 12. 页面六：系统配置

用于配置基础路径和运行参数。

---

## 12.1 浏览器配置

字段：

- 浏览器类型：
  - Edge；
  - Chrome；
- 浏览器路径；
- 用户数据目录；
- Profile；
- 收藏夹路径；
- 是否启动后自动读取收藏夹。

---

## 12.2 项目路径配置

字段：

- 项目根目录；
- 日志目录；
- 临时目录；
- OCR 临时目录；
- 配置文件路径；
- 截图保存目录。

---

## 12.3 运行配置

字段：

- 最大并发数；
- 单账号超时时间；
- 通行证最大重试次数；
- 失败后是否自动重试；
- 全部完成后是否统一校验；
- 是否保存失败截图；
- 是否启用调试模式。

---

# 13. 后端接口预留

UI 需要预留接口，但不要实现真实逻辑。

---

## 13.1 UI → 后端信号

示例：

```python
class LauncherBackendBridge(QObject):
    start_requested = Signal(dict)
    stop_requested = Signal()
    pause_requested = Signal()
    resume_requested = Signal()
    retry_failed_requested = Signal()
    arrange_windows_requested = Signal(dict)
    scan_windows_requested = Signal()
    test_ocr_requested = Signal(dict)
    save_config_requested = Signal(dict)
```

---

## 13.2 后端 → UI 更新方法

示例：

```python
def update_task_status(self, account_id, status, message=None):
    pass

def update_progress(self, total, finished, success, failed, skipped):
    pass

def append_log(self, level, account, message):
    pass

def update_window_list(self, windows):
    pass

def update_ocr_preview(self, image_path, result_text, confidence):
    pass
```

---

# 14. 配置化要求

必须遵守：

1. UI 不写死业务坐标；
2. UI 不写死窗口数量；
3. UI 不写死账号层级数量；
4. UI 不写死浏览器路径；
5. UI 不写死 OCR 区域；
6. UI 不写死分辨率；
7. UI 不写死缩放比例；
8. UI 不写死账号映射；
9. UI 不写死窗口标题模板；
10. UI 不伪造成功日志；
11. UI 不直接操作浏览器；
12. UI 不直接点击游戏窗口；
13. UI 不修改已有稳定后端逻辑；
14. 所有配置都通过配置文件或界面输入传递。

如果必须暂时使用固定参数，必须标注：

```text
当前环境临时参数，需后续配置化。
```

---

# 15. 推荐文件结构

推荐结构：

```text
douluo_launcher/
├── gui_pyside6.py          # 主窗口
├── qt_styles.py            # 样式
├── qt_widgets.py           # 自定义控件
├── qt_models.py            # 表格模型
├── ui_bridge.py            # UI 与后端桥接信号
└── config/
    └── ui_config.json      # UI 配置
```

如果当前项目不方便新增文件，可以先只修改：

```text
gui_pyside6.py
qt_styles.py
```

不要大规模重构项目结构。

---

# 16. 最终交付要求

Gemini 需要交付：

1. 一个完整可运行的 PySide6 UI；
2. 所有按钮可点击；
3. 按钮只触发空接口或提示；
4. 表格、日志、状态栏、配置面板完整；
5. 不接入真实后端；
6. 不影响当前已有上号器稳定逻辑；
7. 给 Codex 留好接口，方便后续接入真实后端。

---

# 17. 最终操作路径

UI 的核心目标是让用户能清楚完成：

```text
选账号 → 选窗口 → 配置排列 → 开始上号 → 查看进度 → 查看失败 → 重试失败
```

优先保证：

1. 界面清晰；
2. 配置完整；
3. 状态明确；
4. 接口预留干净；
5. 不破坏现有逻辑。

---

# 18. 给 Gemini 的重点提醒

请严格按以下原则开发：

- 先做 UI，不碰后端；
- 不要重写稳定模块；
- 不要添加无关功能；
- 不要写死坐标、路径、窗口数量；
- 不要伪造成功；
- 不要把模拟状态当成真实状态；
- 所有按钮都通过 signal / slot 预留；
- 后端接入由 Codex 完成；
- 修改前先确认当前项目已有 UI 文件结构；
- 修改后保证源码模式可运行。
